# 虚拟组网方案调研结论（Tailscale / ZeroTier / EasyTier）

> 结论时间：2026-09-20　环境：这台 Windows 机器 + 校园网（172.16.24.0/21，接入层客户端隔离）

---

## ⚠️ 重要更正（第二次测量后修正）

**第一版结论里我写「Tailscale 装不了」，这是不准确的。** 原因是我漏看了一件事：
**这台机器上跑着 FlClash（翻墙代理），而且 `http_proxy` / `https_proxy` 环境变量是设着的**，
所以我第一批 `curl` 测试全走了代理，结论被代理的路由规则污染了。

分开测之后，真实情况是：

| 目标 | 走 FlClash 代理 | 纯直连 |
|---|---|---|
| `login.tailscale.com`（控制面） | **302 ✅** | 000 ❌ |
| `controlplane.tailscale.com` | **302 ✅** | 000 ❌ |
| `pkgs.tailscale.com`（下载站） | **200 ✅** | 000 ❌ |
| `api.tailscale.com` | **302 ✅** | 000 ❌ |
| `derp1`~`derp10`（中继） | 000 ❌ | 000 ❌ |

**准确的结论是：`*.tailscale.com` 被 SNI 阻断。**

证据（关键）：
```
直连 derp1.tailscale.com（TCP 能连上 159.89.225.99:443，327ms）
  → schannel: failed to receive handshake, SSL/TLS connection failed
直连 cloudflare.com
  → HTTP 301 ✅
```
**TCP 层能连上，只有带 Tailscale SNI 的 TLS 握手被掐掉** —— 这是典型的 SNI 封锁，
不是整个 IP 段被封。

### 所以 Tailscale 到底能不能用？

**能，但有两个前提，而且就算做到了也不会更快：**

1. 控制面必须走代理（实测 ✅ 已通）
2. **DERP 中继也得走代理** —— 现在 `derp*.tailscale.com` 走代理返回 000，
   是因为 FlClash 的规则把它判成了直连。加一条规则让它也走代理，DERP 就能通。

**但即使全通了，延迟也不会改善：**

| 中继节点 | 直连 TCP RTT |
|---|---|
| `derp1.tailscale.com` | 327 ms |
| `derp2.tailscale.com` | 267 ms |
| `derp9.tailscale.com` | 384 ms |

DERP 中继本身就在海外，**和现在的 Cloudflare 隧道（355ms）是同一水平**，
再叠加代理开销只会更慢。而 P2P 直连在国内（对称 NAT + UDP 封锁）大概率打不通。

**所以 Tailscale 解决不了你的延迟问题** —— 不是连不上，是**绕不过物理距离**。

---

## 一句话结论（更正后）

**Tailscale 可以设法连通，但它和 Cloudflare 隧道走的是同样远的路径，延迟一样高。**
**想要低延迟，唯一的办法是让流量别出国——用校园 VPN，或者国内中转。**
**ZeroTier 则确实不可用**（见下文，根服务器 UDP 9993 直连超时）。

---

## 实测证据

### Tailscale：控制面 TLS 被阻断

| 目标 | 结果 |
|---|---|
| `login.tailscale.com` | TCP 能连（259ms），但 **TLS 握手失败**：`schannel: failed to receive handshake, SSL/TLS connection failed` |
| 同上，用 `--resolve` 绕过 DNS 直连 IP `192.200.0.115` | **同样失败** → 不是 DNS 问题，是 TLS 层阻断 |
| `controlplane.tailscale.com` | 同上（294ms 连上，TLS 失败） |
| `derp1` ~ `derp10.tailscale.com`（中继节点） | **全部不可达（000）** |
| `pkgs.tailscale.com`（客户端下载站） | 不可达 |
| 对照：`cloudflare.com` / `baidu.com` | 200 / 301 ✅ 正常 |

Tailscale **同时依赖控制面（登录、密钥交换）和 DERP 中继**，两边都被封 → 装上也无法登录。

### ZeroTier：装好了，但节点永远 OFFLINE（这一条依然成立）

> 注：ZeroTier 的测试是 Node 原生 UDP，**不走 HTTP 代理**，所以结论不受 FlClash 影响。

我实际下载安装了 ZeroTier 1.16.2（12MB，从 `download.zerotier.com` 成功下载），服务正常启动：

```
200 info 2008622301 1.16.2 OFFLINE          ← 节点状态：离线
200 listpeers 62f865ae71 - -1 - PLANET      ← 4 个根服务器，延迟 -1、路径空
200 listpeers 778cde7190 - -1 - PLANET
200 listpeers cafe04eba9 - -1 - PLANET
200 listpeers cafe9efeb9 - -1 - PLANET
```

等了 65 秒依然是 OFFLINE。UDP 9993 到三个根服务器（`50.7.252.138` / `104.194.8.134` / `185.152.66.6`）**全部超时**。

而 `my.zerotier.com`、`download.zerotier.com` 都是 200（网站能开），**说明被封的是数据面 UDP 9993**，不是整个域名。

### EasyTier：官方公共节点地址在文档里是占位符

官方 README 里写的是 `-p tcp://<共享节点IP>:11010`（占位符，真实地址只在网页控制台）。
网上流传的 `public.easytier.top` / `public.easytier.cn` 经 114 / 223.5.5.5 / 1.1.1.1 三个 DNS 查询**全部 ENOTFOUND——这两个域名根本不存在**，那篇教程的地址已失效。

### 蒲公英（贝锐）：国内服务，可达 ✅

`pgy.oray.com` → 200。这是国内厂商的虚拟组网产品，控制面在国内，**不会被封**。
免费版限制 3 个设备（够 3 人开黑），需要注册账号。

---

## 根因（更正版）

第一版我把原因归给「校园网封了海外控制面」，**这个说法不准确**。更准确的描述是：

1. **`*.tailscale.com` 被 SNI 阻断** —— 直连时 TLS 握手被掐，走代理就能通；
2. **ZeroTier 的根服务器 UDP 9993 直连超时** —— 这一条不受代理影响，是真的不通；
3. **就算把 Tailscale 走代理打通了，中继还在海外，延迟和现在一样**。

所以问题不是"能不能连上"，而是**"流量能不能别出国"**。

## 原根因分析（保留备查）：虚拟组网都依赖"协调服务器"

```
你的电脑 ──→ 协调服务器（分配虚拟 IP、交换密钥）──→ 对方的电脑
                    ↑
              海外 → 被校园网封
              国内 → 能通（蒲公英）
              自建 → 需要一台服务器
```

海外协调服务器被封锁是**网络侧策略**，客户端怎么配都绕不过去。
自建协调服务器（EasyTier 共享节点 / Headscale / ZeroTier moon）**需要一台服务器**——
而既然要买服务器，**frp 比自建组网简单得多**。

---

## 所以，可行的方案只剩这些

| 方案 | 延迟 | 成本 | 要装客户端？ | 状态 |
|---|---|---|---|---|
| **校园 VPN** ⭐ | 校园网级 | 0 | 否 | ✅ **已做好**（`start-vpn.bat`） |
| **免费公共 frp** | 20~60ms | 0（邮箱注册，**不实名**） | 否 | ✅ 客户端已备好（`frp/`） |
| ~~自购服务器 + 自建 frps~~ | — | 38 元/年起 | — | ❌ **需实名，未成年走不通**（相关文件已删） |
| 花生壳 / natapp 免费版 | 20~60ms | 0（1GB/月） | 否 | ✅ 可用 |
| **蒲公英免费版** | 20~60ms | 0（3 设备） | **是**，每人要装 | ✅ 可用（需注册） |
| 手机热点 / 同地点 | <1ms | 0 | 否 | ✅ 可用 |
| ~~Tailscale~~ | — | — | — | ❌ **被封** |
| ~~ZeroTier~~ | — | — | — | ❌ **被封** |
| EasyTier 自建节点 | 10~40ms | 38 元/年起 | 是 | ⚠️ 要买服务器，不如直接用 frp |
| Cloudflare 隧道（现状） | 355ms | 0 | 否 | ✅ 已在用 |

---

## 我的建议

**你的核心诉求是"低延迟 + 朋友点开就能玩"。虚拟组网虽然概念上更优雅，但它有两个硬伤：**

1. **海外控制面被校园网封**（Tailscale / ZeroTier 实测都不可用）
2. **每个玩家都要装客户端并注册账号** —— 对"发个链接就玩"的体验是毁灭性的

而 **frp 方案：朋友只要点一个 `http://IP:3000` 链接就能玩，什么也不用装，延迟还更低。**

所以：

- **想省钱** → 先试 **花生壳免费版**（零成本、无需注册客户端给朋友、延迟就能降到几十毫秒）
- **想最省事** → **38 元买台服务器 + frp**（已经全部配好，只差一个 IP）
- **只要 3 个人玩、不介意都装客户端** → 蒲公英免费版

---

## 附：ZeroTier 可以卸掉

它现在是 OFFLINE 状态，装着无害但会多一个虚拟网卡和一个常驻服务。
不需要的话，管理员 PowerShell 执行：

```powershell
Get-Package -Name "ZeroTier One" | Uninstall-Package
```

或到「控制面板 → 程序和功能」里卸载。
