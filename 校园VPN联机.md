# 校园 VPN 联机（推荐方案）

> 2026-09-20 实测。这是目前**最省钱、最省事、延迟最低**的方案。

## 先说结论

**让朋友也连上同一个校园 VPN，然后打开 `http://你的VPN地址:3000` 就能玩。**

- ✅ 零成本
- ✅ **不用实名**（你们本来就有学校账号）
- ✅ 朋友**不用装任何东西**，浏览器打开就行
- ✅ 延迟是**校园网级别**（对比 Cloudflare 隧道的 355ms）

一键启动：**双击 `start-vpn.bat`**

---

## 为什么之前的方案都不行？——查到了真正的原因

你这台机器上装了两套**深信服（Sangfor）**的客户端：

```
C:\Program Files\Sangfor\NAC\bin\nac_agent.exe        ← 网络准入控制（NAC）
C:\Program Files\Sangfor\Ingress3.0.1\                ← SSL VPN 客户端
C:\Program Files\Sangfor\Ingress3.0.1\trumpinj*.sys   ← 内核级网络注入驱动
```

运行中的进程：`nac_agent.exe`、`nac_monitor.exe`、`Ingress.exe`、`IngressMgr.exe`

> 你以为是「蓝盾」，实际是**深信服**。两家都是做校园网准入的，功能类似。

**深信服 NAC 的招牌功能之一就是「应用识别与管控」——按特征识别并阻断 VPN / 代理类工具。**
这就解释了之前的谜团：

| 之前测的 | 结果 | 现在能解释了 |
|---|---|---|
| Tailscale 控制面 | TCP 通但 **TLS 握手被断** | NAC 按应用/SNI 特征拦截 |
| Tailscale DERP 节点 | 全部不可达 | 同上 |
| ZeroTier 根服务器 | UDP 9993 全部超时 | 同上 |
| 国内 114DNS | 8ms ✅ 正常 | 国内普通流量不拦 |

**不是 Cloudflare 的问题，也不是校园交换机隔离的问题——是终端上的 NAC 在按应用特征拦。**

### ⚠️ 重要：不要试图关掉或绕过它

`nac_agent` 是**学校部署的网络安全管控**，绕过它：
- 违反校规，这类客户端**会上报合规状态**，学校那边看得到
- 动了它可能被判定为「破坏计算机信息系统」
- 而且它有内核驱动（`trumpinj*.sys`），硬来容易把系统搞坏

**但用校园 VPN 联机不是绕过——那是学校提供的正常服务，用它完全合规。**

---

## 怎么用

### 第一步：确认 VPN 已连接

连接校园 VPN（就是你平时连的那个客户端）。连上的标志：

```
PPP 适配器 111:
   IPv4 地址 . . . . . . . . . . . . : 192.168.30.11
```

`ipconfig` 里能看到一个「**PPP 适配器**」就对了。

### 第二步：双击 `start-vpn.bat`

它会自动：
1. 找到 VPN 网卡的地址
2. 启动游戏服务（已在跑就直接复用）
3. **把地址复制到剪贴板**并打印出来

```
==============================================================
   让朋友也连上【同一个校园 VPN】，然后打开：

        http://192.168.30.11:3000

   （已复制到剪贴板，也存了一份到 vpn-url.txt）
==============================================================
```

### 第三步：把地址发给朋友

朋友需要做的**只有两件事**：
1. 连上同一个校园 VPN
2. 浏览器打开你发的地址 → 输入 4 位房间号

---

## 注意事项

- **VPN 地址可能每次重连都变**（`192.168.30.x` 里的最后一段），
  所以**每次开局重新跑一下 `start-vpn.bat`**，用新地址。
- 如果朋友打不开，先确认他连的**是同一个 VPN**（同一个学校、同一个入口）。
- 有些学校的 VPN 默认**不允许客户端之间互访**。如果打不开，说明你们学校关了这个策略——
  那就只能走 [国内中转 frp](../frp/README.md) 或 [免费公共 frp](#免费公共-frp服务不用实名)。
- 玩游戏时**别断 VPN**。

---

## 备选：免费公共 frp 服务（不用实名）

如果你的学校 VPN 不让客户端互访，还有个**不用实名**的路子：
用**别人搭好的公共 frp 服务端**，你只当客户端连上去。

> ⚠️ 前提：你的 NAC 不拦 frp。**没测过**——因为 NAC 可能把 frp 也当代理工具拦掉。
> 先试一下才知道。

| 服务 | 隧道数 | 免费限速 | 官网 |
|---|---|---|---|
| **OpenFrp**（社区活跃，推荐先试） | 2 | 12 Mbps | https://www.openfrp.net |
| **SAKURA FRP 樱花映射**（稳定） | 2 | 10 Mbps | https://www.natfrp.com |
| **ChmlFrp** | 4 | 国内 8 Mbps | https://www.chmlfrp.net |
| **Fnat**（标称无限流量） | — | — | https://www.fnat.cc |
| ME Frp 幻缘映射 | 2 | 10 Mbps（实名后 25） | https://www.mefrp.com |

**这些服务只要邮箱注册，不实名也能用**（实名只是提速，不是门槛）。

> 💡 **速度完全不是问题**：我们的游戏笔画数据约 **5 KB/s ≈ 0.04 Mbps**，
> 连最慢的 4 Mbps 都是需求的 100 倍。别为带宽纠结。

### 用法（以标准 frpc 为例）

1. 注册账号（邮箱即可）
2. 在控制台**创建一条 TCP 隧道**：本地 `127.0.0.1:3000`
3. 拿到 **节点地址** 和 **访问密钥（token）**
4. 填进 `frp/frpc.toml`：

```toml
serverAddr = "控制台给的节点地址"
serverPort = 7000
auth.method = "token"
auth.token = "你的访问密钥"

[[proxies]]
name = "drawguess"
type = "tcp"
localIP = "127.0.0.1"
localPort = 3000
remotePort = 0          # 0 = 让服务端自动分配
```

5. 双击 `start-frp.bat`

> 大多数服务也提供**自己的图形客户端**，那个比手写配置更省事，推荐新手用。

---

## 三个方案的最终对比

| 方案 | 延迟 | 成本 | 朋友要装东西？ | 要实名？ | 状态 |
|---|---|---|---|---|---|
| **校园 VPN** ⭐ | **校园网级** | 0 | **不用** | **不用** | ✅ 已配好（`start-vpn.bat`） |
| 免费公共 frp | 20~60ms | 0 | 不用 | 不用 | ⚠️ 可能被 NAC 拦，需实测 |
| 国内中转 frp（自购服务器） | 10~40ms | 38 元/年 | 不用 | **要**（未成年走不通） | ✅ 已配好 |
| 手机热点 / 同地点 | <1ms | 0 | 不用 | 不用 | ✅ 随时可用 |
| Cloudflare 隧道 | 355ms | 0 | 不用 | 不用 | ✅ 已在用 |
| ~~Tailscale / ZeroTier~~ | — | — | — | — | ❌ 被 NAC 拦 |

**先试校园 VPN**——它是最优解。
