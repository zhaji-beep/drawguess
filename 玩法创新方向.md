# 你画我猜 · 玩法创新方向报告

> 2026-09-21
> 基于 `优化报告.md`（10 项已落地玩法）+ `server.js`（1207 行）实际代码结构推演
> ⚠️ 说明：以下判断来自**读代码与报告**，不是实机体感。凡是需要手感验证的结论我都标了出来。

---

## 小规模人数（2 人）重评估

> **2026-09-21 最新结论，以本节为准。**
> 前提变更：现实里长期只有 2 人 → 下方「一~六」各节里依赖 3 人以上的方向
> （团队对抗、抢画模式、观众竞猜）**全部作废**。

### 一 · 先诊断：2 人局到底差在哪

现在的回合结构是「1 人画、1 人猜」。放进 2 人局，会同时暴露三个缺陷：

| # | 缺陷 | 具体表现 |
|---|---|---|
| 1 | **没有观众** | 4 人以上时，乐趣有很大一块来自旁观——看别人猜错、抢答、打趣。2 人时这个维度**完全归零** |
| 2 | **互动是串行的** | 永远一个人动、一个人等。**至少 50% 的时间在纯等待**，而且等的人无事可做 |
| 3 | **没有共同目标** | 纯零和。水平一有差距就一边倒（追赶机制只缓解分数，缓解不了体验） |

### 二 · 由此推出一条设计原则

> **2 人局应该「两人合作对抗系统」，而不是「两人互相对抗」。**

理由：对抗在 2 人下要么一边倒、要么尴尬（赢得没意思、输得难受）；
而**合作能造出共同目标**——"一起撑到第几关""看看这条链歪成什么样"。
这类**共同的回忆**，是 2 人局唯一能补上"没有观众"的东西。

下面所有方向都按这条原则筛过。

**一个先被排除的明显想法**：AI 对手。
零依赖（不装任何 npm 包）决定了我接不了大模型，也做不出"看得懂画"的裁判。
所以「AI 当猜者」「AI 评分」「AI 画手」三条路全部关闭——不要在这上面花时间。

---

### 三 · 方向清单

#### A · 链接即邀请 —— 先做这个

| 项 | 内容 |
|---|---|
| **机制** | A 建房后，复制出去的链接**带房间号**（`?room=1234`）。朋友点开**自动进房**，不用手输那 4 位数字 |
| **人数门槛** | 1 人可建房，2 人成局 |
| **为什么人少关键** | **它不改善玩法，它改善"能不能凑到人"——而这才是你的根源问题。** 现在邀请一次 = 收到链接 → 打开 → 输昵称 → 手输 4 位房间号，四步里最后一步最容易劝退 |
| **工作量** | **约 20 行**（登录页读 query 参数 → 自动填入 → 自动提交）。前端改动，不用重启服务 |
| **风险** | 极低。唯一的点是房间号错误时的报错文案要友好 |

#### B · 接盘画（不清空画布）

| 项 | 内容 |
|---|---|
| **机制** | 回合结束**不清空画布**，下一位画手在上一轮的残留上画新题。清空可以，但要扣分（给出口，别堵死） |
| **人数门槛** | **2 人即可，而且 2 人是最优** |
| **为什么人少好玩** | 人多了反而有概率接到"自己的盘"；**2 人时 100% 是接别人的鬼画符，干扰最强、笑点最密**。这是少见的"人越少越纯粹"的机制 |
| **工作量** | **半天**（改动点已定位：`advanceTurn` / `chooseWord` 保留 strokes，并在 `turnStart` 后补发 `fill`） |
| **风险** | 画面会变脏，画白色物体在残留上更看不清（和已有底色切换可能打架）。做成开关即可 |

#### C · 双词共画（Two-in-One）

| 项 | 内容 |
|---|---|
| **机制** | 画手拿到**两个词**，必须在同一块画布上把它们都表达出来；猜者要**分别猜中两个**才算过。两个空位独立显示 |
| **人数门槛** | **2 人正好**，3 人以上也行 |
| **为什么人少好玩** | 它把"猜一个词"升级成**"两个词怎么共存"的智力题**——画手要设计构图（熊猫在洗衣服？），猜者要拆解画面。**信息密度翻倍，笑点密度也跟着翻倍**，而这块画布本来就只有 2 个人在看，不需要额外的人来"看懂" |
| **工作量** | **约 1 天**（选词发两个 + 判定要猜中两个 + UI 两个空位） |
| **风险** | 画手负担加重，可能太累。建议限制两个词**难度都偏低**（简单+中等），且时长给足 |

#### D · 合作生存（Co-op Endless）

| 项 | 内容 |
|---|---|
| **机制** | 两人一队，**共享一条命**。每过关时间 -3 秒（最低 25 秒），每 3 关难度档 +1（简单→中等→困难）。失败 3 次出局，结算"共同撑到第 N 关" |
| **人数门槛** | **2 人正好** |
| **为什么人少好玩** | **把零和换成共同目标**——失败不再是"我输给你"，而是"我们只撑到第 8 关"。这是唯一能补上"没有观众"的机制层方案。而且人少反而更紧张：**没有第三个队友救场** |
| **工作量** | **约 1 天**（几乎全部复用现有回合机制，只加"难度递增 + 共享生命 + 通关计数"） |
| **风险** | 不碰现有分数系统（用独立指标"通关数"），风险低。要注意难度曲线不能太陡（前期就该有 5~6 关的顺风期） |

#### E · 背靠背同题竞速（Head-to-Head Parallel）

| 项 | 内容 |
|---|---|
| **机制** | 两人拿**同一个词**，**同时**画各自的画布 → **同时**进入猜对方画的阶段 → 先猜中者得分。 |
| **人数门槛** | **正好 2 人**（3 人以上要配对，反而变复杂） |
| **为什么人少好玩** | **把串行变并行，零等待**。而且"同一题两个人的不同解法"本身就是笑点——对比感是天然生成的。**关键：两人各自看自己的屏幕，不需要并排两块画布**，所以手机上完全可行 |
| **工作量** | **约 2 天**（双画布状态 + 同步提交 + 阶段切换） |
| **风险** | 需要新的阶段编排；两端状态必须同步（一方画完另一方还在画时怎么处理）。是本批里最重的 |

#### F · 双向接管画笔

| 项 | 内容 |
|---|---|
| **机制** | 猜的人可以**抢过画笔**补两笔（比如每回合 1 次、限 5 秒），迫使画手在自己画的东西上继续。两人共用一块画布**博弈** |
| **人数门槛** | **2 人正好** |
| **为什么人少好玩** | **把"等待"变成"参与"**——补上缺陷 2（串行等待）。猜者不再只能打字，而是能直接改变局面 |
| **工作量** | **约 1.5 天** |
| **风险** | **权属混乱**：画手的东西被乱画会很不爽。必须给画手"撤销对方补笔"的权力，且限制抢笔时长。建议默认关 |

---

### 四 · 优先级与建议组合

| 顺序 | 方向 | 工作量 | 解决哪个缺陷 | 推荐度 |
|---|---|---|---|---|
| **1** | **A 链接即邀请** | 20 行 | 直接提高凑到人的概率 | ⭐⭐⭐ 立刻做 |
| **2** | **B 接盘画** | 半天 | 制造意外 | ⭐⭐⭐ |
| **3** | **C 双词共画** | 1 天 | 提高信息/笑点密度 | ⭐⭐⭐ |
| **4** | **D 合作生存** | 1 天 | 缺陷 3（没有共同目标） | ⭐⭐ |
| 5 | F 双向接管画笔 | 1.5 天 | 缺陷 2（串行等待） | ⭐⭐ |
| 6 | E 背靠背竞速 | 2 天 | 缺陷 2（最彻底，但最重） | ⭐ |

**建议组合（约 2.5 天）**：**A → B → C**。

- A 是唯一能真正解决"人太少"的动作，且成本几乎为零；
- B + C 都在**不增加人数需求**的前提下把单局的信息量和笑点密度拉上去；
- 三者互不冲突，且都不碰分数系统。

**排在最后的话**：E 背靠背竞速虽然最能解决"串行等待"这个缺陷，但它要新建一套阶段编排，
收益/成本比不如前三项。**先做 A/B/C，跑几局看还缺什么，再决定要不要上 E。**

---

## 实施进度（2026-09-21 更新）

| 方向 | 状态 | 备注 |
|---|---|---|
| **A · 链接即邀请** | ✅ **已实现** | `?room=1234` 自动填房间号；昵称存过则一步进房。真浏览器实测通过 |
| **B · 接盘画** | ✅ **已实现** | 挂在大厅「限制玩法」开关里（**默认关**）；清空残留 -10 分，不会扣成负数 |
| **2 · 猜词温度条** | ✅ **已实现** | 四层判断（很近 / 方向对 / 沾边）；「方向对」每回合限 3 次，用完自动降级；17 项回归测试 |
| **3 · 线条平滑** | ✅ **已实现** | 客户端一阶低通滤波，常量 `STROKE_SMOOTH = 0.34`（想调手感改这一个数） |
| **1 · 断线重连** | ✅ **已实现** | 服务端席位保留（3 分钟）+ 前端自动重连；切后台 / 息屏 / 网络切换回来自动接上，**6 秒内的闪断完全无感**。16 项回归测试 |
| C · 双词共画 | ⬜ 待做 | 一图两词，两个都猜中才过关 |
| 5~8 · 其余 | ⬜ 待做 | 见下方各节 |
| 开发者署名 | ✅ 已完成 | 开发者 炸鸡 · QQ 3627690979 —— 登录页（QQ 可点一下复制）+ 结算页 + 服务端启动横幅 + `package.json` 的 author |

> **本批的两条刻意约束**：
> 1. **不碰分数系统**——现有 5 个加成源已接近复杂上限，新玩法一律「不加分」；
> 2. **保留原有 `close` 消息类型**——所以 e2e 的「接近了私有提示」两项断言零回归。
>
> 全量测试：**374 项断言全部通过**（猜词温度 17 + 接盘画 9 + 断线重连 16 + 邀请链接 4）。
> 温度条改动落在 `server.js`（`WORD_CAT` / `WARM_BUDGET` / `handleGuess`），
> 平滑落在 `public/app.js`（`drawStart` / `drawMove`），UI 在 `index.html` + `style.css`。

---

## 零 · 先给判断：现在缺的不是「更多玩法」

翻完现有实现，我认为真正制约体验的是三件事，都在玩法之外：

### 判断 1 · 画手侧全是「限制」，没有一项「赋能」

已落地的画手相关改动：

| 项目 | 性质 |
|---|---|
| 只用一种颜色 / 一笔画完 / 禁用橡皮 / 盲画模式 | **加难度** |
| 开天眼（揭示一个字，扣 15 分） | **惩罚性补救** |

**没有任何一项是在帮画手「画得出来」。** 而全局体验几乎完全由画手质量决定——画崩了，整桌人都没得玩。

结果是双向恶化：老手觉得手脚被绑住（四个限制开关可以同时开），新手本来就画不好，叠上限制更不敢当画手。

> **画手侧下一个动作应该是赋能，不是再加限制。**

### 判断 2 · 猜词者的动作空间只有「打字」

`server.js` 里猜词者能主动发起的消息只有四条：

```
guess  chat  react  vote
```

而且代码里是这样写的：

```js
case 'guess':
case 'chat':
  handleGuess(room, player, m.text);
  break;
```

**猜词和聊天共用同一个 handler。** 也就是说，整局游戏猜词者的操作就是——在输入框里打字。

已落地的「表情条移出工具栏」之所以效果超预期，正是因为它给猜词者开了第二个动作。**这个方向还有很大空间。**

### 判断 3 · 分数系统已经有 5 个加成源，接近复杂上限

```
猜中基础分 × 难度倍率(DIFF_MULT 1.0 / 1.3 / 1.7)
  + 连击（+12×层，上限 ×5）
  + 落后追赶（最多 +40%）
  + 画作投票（8 分/星，满分 +40）
  + 画手全员猜中（+15%×层）
  − 开天眼（15）
```

玩家算不清自己为什么是这个分。**新玩法优先做「不加分」的**——否则要么分数通胀，要么必须先砍掉一个旧加成。

---

## 一 · P0 方向（改动小、直接改善核心体验）

### 1. 猜词温度条：把「接近了」从文字变成连续反馈

**解决的痛点**
现在猜词者大部分时间在盲猜。已有的「接近了」提示只解决一种情况（猜的是答案的子串），
猜「老虎」而答案是「狮子」时——**同样是一个字都没蒙对，但方向其实百分百正确，游戏却完全不告诉他**。

**怎么结合现有设计**
关键在 `handleGuess` 里那段判定：

```js
if (w.length > 1 && g.length >= minPart && g.length < w.length && w.includes(g)) {
  send(player, { t: 'close', text });
  return;
}
```

**它只是子串包含，不是相似度。** 所以不能直接复用，要新写一个分层打分：

| 层 | 判据 | 提示强度 |
|---|---|---|
| 🔥 很近 | 是答案子串（沿用现有逻辑） | 明说「接近了」 |
| 🌤 方向对 | **同一分类**（`words.js` 已有分类信息） | 只说「方向对了」，**不说哪个分类** |
| 🌥 沾边 | 字数相同 或 字符交集 ≥1 | 只说「思路有偏差」 |
| ❄️ 无关 | 其余 | 不提示 |

> 💡 **这一条最巧的地方**：`words.js` 里词库本来就按 20 个分类组织，
> **不用接任何 AI，零成本拿到语义相关性**。猜「老虎」答案是「狮子」→ 同属动物 → 给「方向对了」。

**实现难度与工作量**
服务端：相似度函数 ~30 行 + 新消息 `{t:'warm', level}` ~10 行。
前端：温度条 UI ~30 行（纯 CSS + 一个 div 改 class）。
**合计约半天。**

**平衡性与风险**
- ⚠️ **「方向对了」会缩小范围**，等于变相送线索。必须限制：
  建议**整局每人最多触发 3 次「方向对」**，用满就只剩「很近」那档；且**不给分类名**，只给「方向」。
- ⚠️ 如果开放给"每次猜测都反馈"，会退化成穷举——玩家挨个试同类词就能锁定。
- ❓ 需要实测的：反馈太灵敏会不会让游戏变简单。**建议先做成房主可开关**，默认开。

**优先级判断**
**最高。** 收益立竿见影（猜词者从盲猜变成推理），工作量最小，不碰分数系统。

---

### 2. 画手线条平滑：让手抖的人也能画

**解决的痛点**
手机上用手指画，线条必然抖。现有「盲画」「一笔画完」还额外放大这个问题——
一笔画完要求一次画对，而大多数人的一次落笔就是一堆锯齿。

**怎么结合现有设计**
前端已有渐进显形（`revealed` 指数逼近）的重绘管线，平滑正好挂在同一条链路上。
两种做法，建议先做简单的：

- **滑动平均**：每 3 个点做一次加权平均，保留笔画形状，抹掉锯齿。~30 行。
- **RDP 简化**：道格拉斯-普克抽稀，笔画更干净，还能顺带压缩传输体积。~60 行。

**实现难度与工作量**
前端 ~60 行，**半天**。

**平衡性与风险**
- ⚠️ **平滑会改变笔迹数据本身**。如果平滑发生在发送前，服务端存的 `strokes` 就是平滑后的——
  这对回放/画廊是好事，但**和已存的旧笔迹格式不兼容**（不过没有持久化，无所谓）。
- ⚠️ 平滑过度会让画手"感觉笔不听使唤"。建议**只平滑抖动、不做拟合**，且指数保守。
- ❓ 手感问题**必须实机验证**，光看代码判断不了。建议做一个灵敏度常量方便调。

**优先级判断**
**高。** 直接改善"画得丑"这个最大体验杀手，工作量小，且和所有现有玩法正交（不冲突）。

---

### 3. 断线重连：刷新一下不该丢分

**解决的痛点**
手机上误触下拉刷新、或者切换 App 回来被回收，**整局分数全归零**。
移动端 `overscroll-behavior: none` 只防了下拉刷新，防不住切后台被系统杀。
这是**手机玩家最容易被劝退的一个点**，而且它发生在最糟的时刻（玩到一半）。

**怎么结合现有设计**
`room.players` 是 `Map<id, player>`。给玩家 ID 加一层稳定化：

1. 前端把 `playerId` 存 `localStorage`
2. 服务端玩家离线后**保留 30 秒席位**（不立即从 `room.players` 删除，只标记 `offline`）
3. 重连时用同一个 ID 复位，恢复分数、连击、是否已猜中

**实现难度与工作量**
服务端 ~50 行（离线标记 + 定时清理 + 重连复位）、前端 ~20 行。**约 1 天。**

**平衡性与风险**
- ⚠️ **身份错乱**：同一台设备换人玩（朋友借手机），旧 ID 会自动接管上一个玩家的分数。
  建议加一个"是不是你？"的确认，或者切换昵称时强制发新 ID。
- ⚠️ **回合状态要完整恢复**：`boardLock`、`usedWords`、`hintMask`、是否已猜中都要回填，
  漏一个就会出现"我明明猜中了但还能再猜"。现有的 `roomState` 已经带了部分状态，可以扩展。
- ⚠️ 30 秒席位会让**房间满员时新玩家进不来**。需要在"房间已满"提示里说明"有人在重连中"。

**优先级判断**
**高。** 纯稳定性收益，不加任何玩法复杂度。**如果是手机玩家为主的场景，这条应该排第一。**

---

### 4. 几何图章库：一键贴圆、方、箭头、五角星

**解决的痛点**
"画不圆"是最高频的挫败。画太阳像土豆、画人像火柴棍，本质不是不会画，是**手指画不出标准几何形**。

**怎么结合现有设计**
- 工具栏已有画笔/橡皮/撤销/清空/开天眼的按钮区，加一组图章是同类扩展
- 图章要作为**一种 draw op**写进 `room.strokes`，否则新加入的人看不到、回放也丢

**实现难度与工作量**
前端 ~120 行（8 种图形用 canvas 原生 API 画，**不需要任何图片资源**，符合零依赖）。
服务端 ~20 行（新增 op 类型，纳入现有 `handleDraw` 的形状校验）。
**约 1 天。**

**平衡性与风险**
- ⚠️ **可能削弱"你画我猜"的灵魂**。图章用多了，画出来的就是符号拼贴而不是"画"。
  建议：**只给基础几何形**（圆/方/三角/线/箭头/星/心/云），**不给"太阳""人""房子"这种语义化图标**——
  后者等于直接给答案。
- ⚠️ 图章和「一笔画完」限制的关系要想清楚：图章算不算一笔？建议**算，且占用唯一那一笔**。
- ⚠️ 可能让"画面变好看"变成套路化。**建议先做成房主开关，默认关**，观察再说。

**优先级判断**
**中高。** 收益明确但风险也明确；做成可开关就不怕。

---

## 二 · P1 方向（需要新交互，但出效果）

### 5. 逐笔分阶段揭晓：让猜词从"看完整画"变成"边看边推"

**解决的痛点**
现在要等画手画完（通常几十秒）才可能猜中。这段时间对猜词者是纯等待。
而且**画手压力集中**——必须画完才有意义。

**怎么结合现有设计**
服务端已有完整的 `strokes` 数组和时间戳，广播时加一层**可见性门槛**即可：

```
第 1 批：前 2 笔可见
之后每隔 N 秒（或每新增 2 笔）解锁一批
```

**早猜中得分更高**——正好接上已有的「⚡ 神速首猜」称号和落后追赶机制。

**实现难度与工作量**
服务端 ~60 行（分层过滤，画手自己不受限）、前端 ~40 行（进度提示"已解锁 3/7 笔"）。
**1~2 天。**

**平衡性与风险**
- ⚠️ **最需要小心的一条**。如果画手起手画的是细节、最后才画主体，早期猜词者完全无从下手，
  反而变成"惩罚画得慢的人"。
- ⚠️ 需要区分"笔"的概念——画手一笔画 30 秒的话，第 1 批就只有那一笔。**建议改用「总长度比例」而不是笔数**。
- ⚠️ 中途加入的人应该看到**当时已解锁的部分**，而不是全部。
- ❓ 节奏是否变好**必须实机验证**，这是本报告里最不确定的一条。

**优先级判断**
**中高，但建议排在 P0 之后，且先做小范围试验（开关 + 只在一局里试）。**

---

### 6. 接盘画：不清空上一回合的笔迹

**解决的痛点**
每回合都是全新的白板，回合之间毫无关系，玩几轮就腻。而**意外感恰恰是这类游戏最稀缺的东西**。

**怎么结合现有设计**
现有代码 `room.strokes` 每回合重置。改成保留上一回合的残留，画手**可以清空但清空要扣分**（比如 -10）。
于是画手面对的是"上一题留下的鬼画符"，要在上面画出正确答案。

> 这几乎必然会产出笑点：上一轮画的是「熊猫」，这一轮要画「洗衣机」，
> 于是你要在一只熊猫的轮廓上加个滚筒。

**实现难度与工作量**
服务端 ~15 行（回合开始不清空、加 `inherit` 标记）、前端 ~25 行（提示条 + 清空扣分确认）。
**半天。**

**平衡性与风险**
- ⚠️ **残留笔迹是干扰，游戏变难**。必须给"清空"的出口（扣分即可，不要禁用）。
- ⚠️ 残留会让画面变脏，**画白色物体在残留上更看不清**——和已有的底色切换功能可能会打架。
- ⚠️ 建议只在**连续回合**继承，换主题时不继承（否则语义混乱）。
- ✅ 风险可控，因为**做成开关就行**，喜欢的人开，不喜欢的人关。

**优先级判断**
**中高。** 单位成本最低的笑点来源，半天工作量，且不碰分数系统。

---

### 7. 跨局「今晚最佳」+ 宿敌机制

**解决的痛点**
一局打完，除了分数什么都没有，**没有"再来一局"的理由**。玩两局就散了。

**怎么结合现有设计**
现有结算页已经算了称号和统计（本局出题 N 个、零重复、全场最佳画作）。加**跨局持久化**：

- **「今晚最佳」榜单**：`localStorage` 存多局累计（最高分、最佳画作、最多首猜），结算页多一块
- **宿敌机制**：上一局第一名在下一局被标记，**所有人对他的追赶加成翻倍**——
  直接复用已有的 CATCHUP 机制，不用新写分数规则

**实现难度与工作量**
前端 ~80 行（localStorage 结构 + 榜单渲染）、服务端 ~30 行（宿敌标记 + 追赶系数）。**约 1 天。**

**平衡性与风险**
- ⚠️ 宿敌机制会**持续打压强者**，连续几局第一的人会很难受。建议**限定触发强度**（只翻倍不叠乘）并允许房主关。
- ⚠️ `localStorage` 在换浏览器/清缓存后清空，需要**明确说明数据存在本地**，别让用户以为是账号数据。
- ⚠️ 榜单要让所有参与者都上榜（不只是第一名），否则对弱者是负反馈。

**优先级判断**
**中。** 对"留存"价值大，对"单局体验"没帮助。人多开黑的场景优先。

---

### 8. 团队对抗（双画手分区）

**解决的痛点**
现在永远是"1 人对 N 人"的星形结构。**2 人局时尤其单调**（就是轮流 1v1），没有任何博弈感。

**怎么结合现有设计**
`room.strokes` 从「数组」升级为 `Map<drawerId, strokes>`，广播时带 `ownerId`——
这正好是《优化报告》P1-7「抢画模式」的同一个底座，**两个玩法共用一套改造**。

**实现难度与工作量**
服务端 ~200 行（队伍结构、双画布下发、回合驱动）、前端 ~250 行（双画布布局）。
**3~5 天，是当前所有方向里最重的。**

**平衡性与风险**
- ⚠️ **手机端根本放不下两幅画**。390px 宽下并排两块画布各自只有 180px，没法画。
  必须做成"上下堆叠 + 可切换"，或者干脆限定**电脑端才开放**。
- ⚠️ 两队看不同的画，**聊天区会语义分裂**（"你画的那是啥"指代不明）。需要给聊天消息附带队伍标记。
- ⚠️ 队伍人数不整除时会有空位，需要处理。
- ⚠️ 复杂度高，**很可能改完之后玩的人反而觉得麻烦**。

**优先级判断**
**低（建议先不做）。** 工作量最大、风险最高、且目标场景（人少时的单调）用方向 6/7 更便宜地解决。
**除非明确要做 5~8 人的固定车队，否则不值得。**

---

## 三 · P2 方向（可以做，但不是现在）

### 9. 回放 / 画廊长图
`room.strokes` 本来就完整存了每回合笔迹，局末用离屏 canvas 按时间重放并拼成一张长图，
直接可以分享到群里。**~2 天**，零服务端改动。
价值在于**把一次性的游戏变成可传播的内容**——别人看到图才会想来玩。

### 10. 协作求生模式
全员共画一幅，在时限内让"裁判"满意。**问题：零依赖环境下没有裁判。**
只能退化成"房主点通过"，那就不是玩法而是体力活。**建议不做。**

---

## 四 · 建议降级或不做的

| 项 | 来源 | 我的判断 |
|---|---|---|
| **AI 裁判**（大模型判断画得像不像） | 优化报告 P2-13 | **不做。** 破坏零依赖是其次，**更严重的是给"画"打分本质是社交伤害**——已经有一个 1~5 星投票在承担这个风险了，不要再来一个 |
| **投票细化多维度**（像不像/好笑/用心） | 优化报告 P3-17 | **不做。** 投票窗只有 3.9 秒，塞三个维度没人投得完。要改就先把窗口拉长，但那会拖慢节奏 |
| **提示道具分档**（首字提示/偏旁提示） | 优化报告 P0-1 | **降级。** 和方向 1「猜词温度条」功能重叠——都在给猜词者线索。**二者选一，我推荐温度条**，因为它不依赖"偏旁表"这种需要维护的数据 |
| **抢画模式**（2~3 人同时画） | 优化报告 P1-7 | **降到 P2。** 底座和方向 8 相同，但同样有"手机放不下多块画布"的硬伤 |

---

## 五 · 优先级总表

| 顺序 | 方向 | 工作量 | 碰分数系统 | 核心收益 | 前置依赖 |
|---|---|---|---|---|---|
| **1** | 断线重连 | 1 天 | 否 | 手机玩家不再被劝退 | 无 |
| **2** | 猜词温度条 | 半天 | 否 | 猜词从盲猜变推理 | 无 |
| **3** | 线条平滑 | 半天 | 否 | "画得丑"直接改善 | 无 |
| **4** | 接盘画 | 半天 | 可能（清空扣分） | 单位成本最低的笑点 | 无 |
| **5** | 几何图章库 | 1 天 | 否 | 画不圆的问题 | 无 |
| **6** | 跨局榜单 + 宿敌 | 1 天 | 是（复用追赶） | 给"再来一局"的理由 | 需先有 1 局的稳定体验 |
| **7** | 逐笔分阶段揭晓 | 1~2 天 | 是（早猜高分） | 猜词参与感 | **需实机验证节奏** |
| **8** | 回放/画廊长图 | 2 天 | 否 | 可传播性 | 无 |
| — | 团队对抗 / 抢画 | 3~5 天 | 是 | 多人局结构升级 | **建议暂缓** |

**如果只做一件事**：做 **1（断线重连）**——它不产生任何新玩法，但它消除的是**最容易被劝退的瞬间**。
**如果只做一件能明显提升乐趣的事**：做 **2（猜词温度条）**，半天工作量，猜词者的整局体验会变。

**建议的执行组合（约 2.5 天）**：断线重连 → 猜词温度条 → 线条平滑 → 接盘画。
这四项全部不冲突、不碰分数平衡，做完整体体验会明显不一样。

---

## 六 · 需要实机验证才能定论的三件事

我读代码能判断"能不能做"，判断不了"好不好玩"。以下三条必须真跑几局：

1. **逐笔揭晓的节奏**——会不会惩罚画得慢的人
2. **线条平滑的手感**——平滑到什么程度算"笔听话"
3. **图章库会不会毁掉游戏的味道**——会不会变成符号拼贴

建议这三项都做成**房主可开关**，先默认关，观察再定。
