# 联机方案（最终版）

> 2026-09-20 定稿
> 结论：**用 Cloudflare 隧道**。下面说清楚为什么，以及怎么用。

---

## 怎么玩（两步）

**① 双击 `start-public.bat`**

会弹出黑窗口，建立隧道，然后打印一个地址并复制到剪贴板：

```
==============================================================
   把这个地址发给朋友，他们用浏览器打开就能玩：

        https://xxxx-xxxx-xxxx.trycloudflare.com

   （已复制到剪贴板，也存了一份到 public-url.txt）
==============================================================
```

**② 把地址发给朋友**

朋友浏览器打开 → 输入你的 4 位房间号 → 开玩。
**朋友不用装任何东西，不用注册，不用实名。**

> ⚠️ **别关那个黑窗口**，关了隧道就断了。
> 地址**每次启动都会变**，所以每次开局都要重新发一次。

---

## 为什么最后是 Cloudflare

我把能试的都试了一遍，结论如下（都是实测，不是推测）：

| 方案 | 延迟 | 不实名能用？ | 朋友要装东西？ | 结论 |
|---|---|---|---|---|
| **Cloudflare 隧道** | **355ms** | ✅ 能 | ❌ 不用 | ✅ **零门槛，保底方案** |
| **SakuraFrp 樱花** | 20~60ms | ⚠️ 要实名，**但 16 岁能过** | 不用 | ⭐ **1 元试一下，优先** |
| **阿里云服务器 + 自建 frp** | 10~40ms | ⚠️ 要实名，**但 16 岁能过** | 不用 | ⭐ 38 元/年起，最稳 |
| OpenFrp | ~265ms | ⚠️ 只能拿**海外**节点 | 不用 | 香港节点和 Cloudflare 一样远，没优势 |
| 腾讯云服务器 | 10~40ms | ❌ **明确要 18+** | 不用 | 走不通 |
| 花生壳 / 蒲公英 / natapp | 20~60ms | ⚠️ 要实名，年龄门槛未确认 | — | 可试 |
| Tailscale | ~300ms+ | ✅ | 要装+注册 | 中继在海外，**比 Cloudflare 还慢** |
| ZeroTier | — | ✅ | 要装 | 根服务器被拦，节点永远 OFFLINE |
| 蒲公英虚拟组网 | 20~60ms | ✅ | **要装+注册，仅 3 台** | 体验代价太大 |

**核心原因**：国内节点受监管要求必须实名；不实名只能走海外，而海外路径不管走谁家都是 ~185ms 起步（实测：香港 HKIX 184ms、Cloudflare 边缘 185ms），**差别不大**。

**所以 Cloudflare 隧道是「不实名 + 朋友零安装」条件下的最优解。**

### ⭐⭐ 但如果你已满 16 周岁，国内 frp 是通的！

之前我以为"未成年一律不能实名"，**这个判断错了**。查了各家官方规则：

| 平台 | 年龄要求 | 16 岁能过吗 |
|---|---|---|
| **腾讯云** | 明确「**年满 18 周岁**」 | ❌ 不能 |
| **阿里云** | 只排除「**14 周岁以下**」 | ✅ **能**（支付宝授权认证，扫码即可，不用手输身份证号） |
| **SakuraFrp 樱花** | 未写年龄限制 | ✅ 大概率能（支付宝 APP 刷脸） |
| **OpenFrp** | 同类 | ✅ 大概率能 |

**依据**：支付宝官方规则——「**16–18 周岁：若持有身份证，可自主完成实名认证**，开通完整功能受限账户」。
也就是说 **16 岁以上可以自己实名支付宝**，不用家长出面。

**两条可行路线（都比 Cloudflare 快一个数量级）：**

| 路线 | 成本 | 延迟 | 步骤 |
|---|---|---|---|
| **樱花 frp**（推荐先试） | **1 元**实名费 + 免费额度 | 20~60ms | 花 1 元买实名资格 → 输姓名证件号 → 支付宝刷脸 → 建隧道 |
| **阿里云服务器 + 自建 frp** | 38 元/年起 | 10~40ms | 支付宝授权实名 → 买 38 元档 → 部署 frps |

**📖 阿里云这条路的完整步骤（含所有链接）：见 [`frp/README.md`](frp/README.md)**
文件都已经备好了（`frpc.exe` / `frps-linux-amd64` / 双端配置 / `start-frp.bat`），**只差你买服务器填个 IP**。

**建议先试樱花**——1 块钱，失败也不心疼。
注意：**认证完成后 1 元不退**（认证前可 7 天无理由退）。另外樱花要求**必须有手机**（要扫码刷脸）。

> ⚠️ 不确定的地方：支付宝 16-18 岁开的是「**受限账户**」，第三方刷脸认证能不能用，
> 官方文档没写死。**1 块钱试一下就知道**，比继续猜强。

---

## 如果哪天条件变了

这些方案随时可以切回来（脚本都留着）：

| 情况 | 用什么 |
|---|---|
| 你和同学**在同一个地方** | `start-game.bat`，直接连局域网地址（延迟 <1ms） |
| 有同学**家里宽带是公网 IP** | 让他当房主，端口映射 3000，延迟 20~50ms |
| 有同学**也有校园 VPN** | `start-vpn.bat` |
| 你**成年了 / 完成实名** | 国内 frp，延迟能到 10~40ms（详见 `虚拟组网-调研结论.md`） |

### ⭐「有公网 IP 的同学当房主」值得留意

这是**不实名、不花钱、延迟又低**的方案。电信/联通家庭宽带很多有公网 IPv4。

**怎么查**：百度搜「IP」，如果显示的公网 IP 和路由器 WAN 口显示的 IP **一样**，就是公网 IP。

有的话让那个同学：双击 `start-game.bat` → 路由器映射外部 3000 → 他电脑 3000 → 把 `http://他的公网IP:3000` 发出来。

---

## 常见问题：能不能直接买个公网 IP？

**结论：买公网 IP 一样要实名，帮不上你。**

| 途径 | 要不要实名 | 价格 | 对你有用吗 |
|---|---|---|---|
| **云服务器的公网 IP** | ❌ 要（= 买服务器） | 38 元/年起 | 走不通 |
| **运营商给家庭宽带加公网 IP** | ❌ 要**宽带账号持有人本人** | **通常免费** | ⚠️ 有条件（见下） |
| 单独买 IP 段 | ❌ 企业资质 | 极贵 | 别想 |

### 唯一可能免费的路：家庭宽带申请公网 IP

三大运营商政策：

| 运营商 | 政策 |
|---|---|
| **联通** | 打 10010 或联通 APP 提工单，说明合理需求（如"远程访问家里的 NAS"）即可，**免费** |
| **电信** | 打客服电话也能办，但**政策在收紧**，部分地区开始拒绝 |
| **移动** | 基本不给（默认就是运营商级 NAT，`100.64.0.0/10` 网段） |

**但有三个前提，缺一不可：**

1. **要宽带账号持有人本人去办** —— 也就是**你家长**。未成年不能自己申请。
2. **公网 IP 属于「那条宽带」** —— 所以**游戏必须跑在那条宽带的网络里**。
   你在学校、游戏在学校电脑上 → **这个 IP 帮不上你**。
   除非：家里有台常开的电脑跑游戏，你和朋友都连到家里那个 IP。
3. 拿到的通常是**动态**公网 IP，重启光猫可能变，需要配 DDNS。

**所以对"在学校开黑"这个场景，这条路基本没用。** 除非你寒暑假回家玩。

### 更实际的：让有公网 IP 的同学当房主

同一个道理——**谁家宽带是公网 IP，就让他在自己家跑游戏**。
他双击 `start-game.bat`，路由器映射 3000 端口，把 `http://他家公网IP:3000` 发出来。
**不实名、不花钱、延迟 20~50ms、所有人不用装东西。**

**怎么查自家有没有**：百度搜「IP」，和路由器 WAN 口显示的 IP 对比——**一样**才是真公网 IP。
（如果 WAN 口是 `100.64.x.x` ~ `100.127.x.x`，那是运营商级 NAT，不是公网。）

---

## 出问题怎么办

**朋友打不开地址**
1. 你的黑窗口还开着吗？（关了 = 断线）
2. 地址是不是旧的？（每次启动都变，重新发）
3. 让朋友刷新一下，Cloudflare 隧道刚建立时可能要等几秒

**能打开但一直转圈**
游戏服务没起来。先双击 `start-game.bat`，确认 `http://localhost:3000` 自己能玩。

**画起来一顿一顿的**
355ms 的延迟就这样，改不了——顶栏右上角有实时延迟指示器，红色就是公网偏高。
已经做过的优化：接收端渐进显形抹平抖动、落笔提速、坐标压缩。**能榨的都榨了。**

**想换回局域网玩**
双击 `stop-game.bat` 关掉，然后 `start-game.bat`。

---

## 文件说明

| 文件 | 用途 |
|---|---|
| **`start-public.bat`** | **常用** — 建公网隧道（朋友直接点链接玩） |
| `start-game.bat` | 只开本地（局域网 / 热点 / 端口映射用） |
| `start-vpn.bat` | 校园 VPN 联机（朋友也有校园 VPN 时才用） |
| `stop-game.bat` | 关掉服务 |
| `test-all.bat` | 跑全部测试（315 项） |
| `虚拟组网-调研结论.md` | 所有试过的方案 + 实测数据（留档） |
| `校园VPN联机.md` | 校园 VPN 方案的详细说明 |
| `优化报告.md` | 词库 / 玩法 / 多端适配 / 延迟优化的完整报告 |
