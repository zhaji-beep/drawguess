# 你画我猜 · 词库与玩法优化报告

> 项目路径：`C:\Users\Administrator\DrawGuess`（启动方式不变：双击桌面的「你画我猜.bat」或 `start-game.bat`）
> 技术栈不变：零依赖，Node 原生 `http` + 手写 WebSocket，`node server.js` 即跑

---

## 一、问题定位：为什么"同一个词老被抽中"

原来的选词函数只有 6 行：

```js
function pickWords(n) {
  const byD = { 1: [], 2: [], 3: [] };
  shuffle(WORDS.slice()).forEach((w) => byD[w[2]].push(w));   // 每次都从全库重新洗牌
  ...
}
```

**每一回合都是从全库均匀随机抽，服务端完全不留"这个词出过"的记忆。**
于是这变成一个标准的"生日悖论"问题：词库越小、抽得越多，撞车概率涨得越快。

把改造前的状态（148 词库 + 均匀随机）跑 2000 局模拟，实测：

| 一局回合数 | 平均重复率 | 出现重复的对局占比 |
|---|---|---|
| 6 回合（2 人 3 轮） | 1.7% | 10.1% |
| 12 回合（2 人 6 轮） | 3.7% | **37.0%** |
| 24 回合（4 人 6 轮） | 7.5% | **85.9%** |
| 60 回合（长局/连开多局） | 17.6% | **100%** |

也就是说：**4 人局打满 6 轮，约 86% 的概率会遇到重复词**——这跟体感完全对得上。

---

## 二、词库扩充：148 → 578 词，7 → 20 个分类

词库从 `server.js` 里抽出来，独立成 **`words.js`**，按「分类 → 难度」两级组织，后续加词只要往对应数组里塞字符串即可。

**新增 430 个词，新增 13 个分类。**

| 分类 | 词数 | | 分类 | 词数 | | 分类 | 词数 |
|---|---|---|---|---|---|---|---|
| 动物 | 53 | | 影视动漫 ★ | 26 | | 校园 ★ | 19 |
| 物品 | 53 | | 运动 ★ | 24 | | 建筑 ★ | 18 |
| 动作 | 51 | | 交通 ★ | 23 | | 科技 ★ | 18 |
| 食物 | 46 | | 节日 ★ | 22 | | 童话 ★ | 16 |
| 自然 | 43 | | 家居 ★ | 21 | | 乐器 ★ | 15 |
| 人物 | 43 | | 水果蔬菜 ★ | 32 | | 天文 ★ | 11 |
| 成语 | 35 | | | | | 医疗 ★ | 9 |

★ = 本次新增分类，合计 20 个分类 / 578 词
难度分布：简单 202 · 中等 233 · 困难 143

选词原则（写在 `words.js` 头部注释里）：
1. **可画性优先**——尽量选有具体形象、能落笔的词，抽象词只集中在「成语」；
2. **同类内难度递增**——不会一上来就抽到"分子料理"这种没法画的；
3. **全库禁止重词**——启动时自动校验，发现重词会在控制台告警。

服务端启动横幅现在会直接报词库规模：

```
   词库：共 578 个词 / 20 个分类（简单 202 · 中等 233 · 困难 143）
   选词：房间内已用词硬排除 + 跨房间 80 词软冷却，池子刷空后自动重洗
```

---

## 三、选词引擎 v2：三层防重复（核心改动）

引擎独立成 **`picker.js`**，与 `server.js` 解耦，可单独压测。

### 三层防护

| 层 | 机制 | 解决的问题 |
|---|---|---|
| ① 房间级硬排除 | `room.usedWords` 记录本局出过的词，**直接不进候选池** | 同一局内绝不重复 |
| ② 跨房间软冷却 | 全局维护最近 **80 个**出现过的词，命中者抽中权重降到 **0.15** | 换房间/重开一局不会又撞上刚画过的词 |
| ③ 换题排除 | 「换一批」时排除本回合已展示过的候选 | 不会"换了个寂寞"，也不会和刚放弃的候选撞车 |

### 两个体验优化

- **难度配比**：3 个候选尽量覆盖 易 / 中 / 难（实测 100% 命中），避免三个候选全是"成语"。
- **分类去重**：同一批候选尽量来自不同分类（实测 100%），避免"熊猫 / 大象 / 老虎"三连。

### 一个容易踩的坑（已修）

困难词只有 143 个，会被**先抽光**。原来的重洗条件是"候选池总数见底"，结果困难词用完后、重洗触发前的这段时间，候选会悄悄退化成"简单+中等"。
现在重洗条件加了第二条：**任一难度在词库里明明还有词、却已被抽空，就立刻重洗**（保留最近 1/4 作为冷却，其余回收）。

### 效果对比（每档 2000 局模拟）

| 一局回合数 | A 老库148 + 老算法 | B 新库578 + 老算法 | C 新库578 + v2 引擎 |
|---|---|---|---|
| 6 | 1.7% / 10.1% | 0.5% / 2.9% | **0.0% / 0.0%** |
| 12 | 3.7% / 37.0% | 0.9% / 10.3% | **0.0% / 0.0%** |
| 24 | 7.5% / 85.9% | 1.8% / 36.0% | **0.0% / 0.0%** |
| 60 | 17.6% / 100% | 4.9% / 96.3% | **0.0% / 0.0%** |

> 格式：平均重复率 / 出现重复的对局占比

**结论：单靠扩词库只能把 12 回合的重复率从 37% 压到 10.3%；加上 v2 引擎后，24 回合内 2000 局零重复。**

---

## 四、已落地的玩法创新（共 10 项）

### 4.1 主题词库（房主可选，20 个分类）
大厅新增主题按钮，房主可多选。选中后本房词池收窄到对应分类，词库规模实时显示（如"动物 + 食物（99 词）"）。非房主点击无效。

### 4.2 🎲 随机 3 类 / 📅 每日主题
两个快捷主题，省掉"每次手动挑分类"的麻烦：
- **🎲 随机 3 类**：房主一点，服务端从 20 个分类里随机抽 3 个，**开局前保持不变**（不会每次刷新都变），开局时定稿。
- **📅 每日主题**：用日期做种子（`mulberry32`），**同一天所有房间拿到同一套主题**，形成"今天大家都画动物"的仪式感。

快捷主题与普通分类**互斥**——选了一键主题会自动清掉手动勾选，避免语义打架。

### 4.3 自定义词库导入
房主可在大厅粘贴一批词（逗号 / 换行 / 分号分隔），**并入本房词池**。
服务端清洗规则：去空白、只留中英文数字、**限长 8 字**、自动去重、单房上限 200 个；难度按字数折算（≤2 字简单 / 3 字中等 / ≥4 字困难）。
适合班级、公司、家庭聚会——把内部梗加进去，画起来才够味。

### 4.4 限制玩法开关（服务端强制）
房主可开三种限制，**服务端在 `handleDraw` 里强制校验，前端禁用只是体验层**：

| 开关 | 行为 |
|---|---|
| 只用一种颜色 | 画手第一笔的颜色被锁定，后续任何颜色请求都会被**改写成锁定色**下发 |
| 一笔画完 | 第一笔**抬笔即锁板**（见下），第二笔 `begin` 直接拒绝，撤销可解锁；同时禁掉「清空画布」（否则等于绕过） |
| 禁用橡皮 | 橡皮模式被拒绝，提示改用「撤销」 |

**「一笔画完」的锁板反馈**：只靠服务端拒绝第二笔是不够的——画手看不到任何变化，会以为是自己没画上，然后一直徒劳地画。
现在改成：`end`（抬笔）时服务端立刻广播 `boardLock {locked:true}`，前端同步做三件事——画布加 `.locked` 类（光标变禁止）、浮出提示条（画手看到「你的一笔已用完，交给队友猜吧」，猜词者看到「画手的一笔已用完」）、`pointerdown` 直接拦截。
`boardLocked` 也写进了 `roomState`，所以**中途加入或断线重连的人状态不会错乱**；撤销后自动解锁，重新拿回这一笔。

### 4.5 画作投票 + 最佳画作奖
回合末的结算停留时间被拆成 **投票窗 + 展示窗**（真实运行 3.9s + 2.6s，**总时长 6.5s 不变**，不影响原有节奏）：
- 非画手给画作打 1~5 星，**画手不能给自己打分**，一人一次；
- 均分 × 8 → 画手的画作分（满星 +40）；
- 局末评出 **⭐ 全场最佳画作**，并单独设立同名称号。

### 4.6 连击加成 Combo
连续回合猜中累积连击，每层 **+12 分**，上限 **x5**（防止长局滚成天文数字）。猜错 / 没猜中则连击归零。玩家列表实时显示 `🔥3`。
画手也有连击：连续"全员猜中"额外 **+15% × 层数**。

### 4.7 落后追赶（橡皮筋机制）
分数落后第一名越多，猜中得分越高，**最多 +40%**（落后 600 分吃满）。防止一边倒，让后半局仍然有得打。聊天区显示"追赶 +23%"。

### 4.8 画手道具「开天眼」
工具栏新增 `👁 开天眼`。画手卡住时可主动揭示一个字，代价是本回合收益 **-15 分**，每回合限 1 次。
解决了"画手画崩了只能干等到超时"的尴尬。按钮用后置灰并显示扣分。

### 4.9 结算称号 + 词库统计
局末按唯一第一名颁发：🎨 神笔马良 / ⭐ 全场最佳画作 / ⚡ 神速首猜 / 🧠 最强大脑 / 🎉 气氛担当。
结算页同时显示：**本局出题 N 个 · 不重复 N 个 · 零重复 🎉 / 词库规模 / 本局已消耗 / 池子重洗次数 / 全场最佳画作**。

### 4.10 修了一个隐藏 Bug：表情互动只有画手能用
反应表情条原来放在绘图工具栏 `#tools` 里，而猜词时 `#tools` 会被整体置灰禁用——**猜词的人根本点不了表情**。
现已把表情条移出工具栏，成为独立一行，所有人都能发。这一条对"互动氛围"的提升其实比想象中大。

### 4.11 换词次数可配置（默认 3 次）
原来「换一批」写死 2 次，抽到不擅长的词只能硬着头皮画。
现在变成房主可调项：大厅「换词」分段控件提供 **2 / 3 / 5 次**三档，**默认 3 次**（比原来宽松一档）。
服务端 `settings.reroll` 会夹在 0~6 之间，`advanceTurn` 每回合从设置里取额度，不再是常量。

### 4.13 盲画模式（限制玩法第四项）
画手**看不到自己刚画的东西**——笔迹延迟 **3 秒**才显形。猜词者不受影响（不然没人看得出画的是什么，笑点就没了）。

实现要点：
- 前端在 `render()` 里过滤：`blindDraw && isDrawer` 时跳过「出生不足 3 秒」的笔迹，正在画的那一笔也不画；
- 每条本地笔迹记 `bornAt`，**服务端下发的历史笔迹不带 `bornAt` → 直接可见**（中途加入的人不会被莫名其妙地清空画布）；
- 400ms 的轻量定时器负责让笔迹「到点显形」，不在盲画状态时自动停掉，不白烧 CPU；
- 画布下方浮出角标「🙈 盲画模式 · 你的笔迹 3 秒后才显形」——**让画手知道不是画丢了，是故意的**（这条很关键，否则会被当成 bug）。

### 4.12 登录页分享地址可复制
原来登录页那行地址**根本没法复制**，两个原因叠在一起：

1. 全局 CSS 上有 `body { user-select: none }`——鼠标压根选不中那段文字；
2. 显示的是 `location.host`，**没有协议头**，复制出去粘到浏览器还得自己补 `https://`。

现在改成：

- 显示 `location.origin`（**带 `https://` / `http://`**），复制出去直接能用；
- 地址旁加了独立「复制」按钮，**点地址本身也能复制**；
- 给 `.share-url` 和 `.room-code` 显式加回 `user-select: text`，想手动选也行；
- 复制后按钮变「已复制 ✓」再自动复原。

顺带修了一个只在局域网暴露的坑：`navigator.clipboard` **只在安全上下文（https / localhost）可用**，局域网走 `http://172.x.x.x:3000` 时它是 `undefined`，原来的复制逻辑在那个场景下是坏的。现在统一走 `copyText()`：优先 clipboard API，非安全上下文**自动回退到 `document.execCommand('copy')`**。房间号的复制也换成了同一套。

同时把登录页副标题从「局域网联机」改成「**局域网 / 公网联机**」——现在两种方式都支持，原来那句是误导。

---

### 4.14 画布底色可切换（画白色物体不再隐形）

**问题**：白色笔在白色画布上等于隐形——想画饺子、雪人、云这类白色物体根本没法画。
（白色本来就在调色板里，问题纯粹是「白底上看不见白」。）

**做法**：工具栏加了一组**方形底色选择器**（和圆形颜色点区分开），三档：

| 底色 | 用途 |
|---|---|
| **白** `#ffffff` | 默认 |
| **浅灰** `#e9eef4` | 白色物体有对比，整体仍偏亮 |
| **深色** `#2b2f38` | 白色笔迹最清楚，画白色物体首选 |

几个关键细节：

- **底色同步给所有人**：画手改了，猜词者的画布也跟着变（不然两边看到的不一样，没法猜）。
- **每回合重置成白色**：否则上一局的深色底会莫名其妙留给下一位画手。
- **橡皮擦跟着底色走**：原来橡皮擦硬编码画 `#ffffff`，在深色底上会留下白色涂抹痕。
  现在改成**用当前底色绘制**，所以换底色不会留下"擦不干净"的痕迹，
  而且**深色底上橡皮擦正好当白笔用**。
- **不算笔画**：换底色不受「一笔画完」限制，也不占用笔画数。
- 迟到的人收到 `fill` 时会一并带上当前底色。

---

### 4.15 修了两个「设置开关」的 bug

**① 限制开关连点会"关不掉"**

前端开关读的是 `S.room.limits`（服务端回包后的缓存）。**快速点两下（开→关）时，第二下读到的还是旧值**，
`!false` 又算成 `true` —— 结果就是「明明关了，进游戏还是开着」。

改成**以按钮自身的 `on` 状态为准**（本地乐观更新），不依赖服务端回包，连点也正确。

**② 开局后改设置静默失败**

服务端原来是 `if (player.id !== room.hostId || room.state !== 'lobby') return;` ——
房主在游戏里改设置，**服务端直接忽略，不给任何反馈**，用户只能以为"改了但没生效"。

现在会明确回一句：`游戏已经开始，设置只能在等待大厅里改`。

---

### 4.16 真浏览器端到端验证（不是只跑单测）

改动多了容易互相影响，所以用 headless Chrome 真的走了一局：

```
=== 第 1 回合：浏览器当画手 ===
  ✓ 换底色会同步给对手（op:bg）   -> 对手已收到
  ✓ 白笔笔画同步到对手            -> begin,pts×13,end
  ✓ 白色笔画颜色正确              -> #ffffff
  ✓ 连点两下后开关是「关」        -> on=false（bug 修复生效）
```

截图：`screenshots/canvas-dark-white.png`、`screenshots/playthrough/drawing-white-on-dark.png`

**为什么值得单独做这一步**：`dom.js` 用 jsdom 跑，它**没有真正的布局和渲染引擎**，
canvas 也是打桩的。所以"底色换了能不能看见"这类问题，只有真浏览器能回答。

---

## 五、后续玩法建议（按性价比排序）

### P0 · 改动小、收益大

1. **提示道具分档**
   除了付费的「开天眼」，再给免费的「首字提示」（时间剩 30% 自动）和「偏旁提示」（显示题目首字的偏旁）。
   *实现要点*：`hintMask` 增加 `partial` 模式，按部首表返回提示。

2. **词库健康度巡检 + 自动补词**
   定期跑 `test/wordpool.js`，若某个分类被抽空太频繁（`recycledCount` 偏高）就提示补词。
   *实现要点*：结算时把 `recycled` 上报到日志，累积统计。

3. **移动端优化**
   画布手势与页面滚动冲突、工具栏在窄屏要折叠（现在限制开关一多，工具栏会换行占两行）。

4. **断线重连**
   现在刷新页面就丢分。可用 `localStorage` 存 `playerId`，服务端保留 30 秒席位。

### P1 · 需要新交互，但很出效果

5. **盲画模式**
   画手看不到自己刚画的东西（笔画延迟 3 秒才回显）。笑点密集，几乎不用改服务端，只需前端在画手侧延迟渲染。

6. **接力画**
   每 15 秒画手自动换人、题目不变，最后一个人画完才开始猜。适合人多（6~10 人）的房间。
   *实现要点*：在 `chooseWord` 之后起一个 `relayTimer`，到点把 `drawerId` 轮转，`strokes` 不清空。

7. **抢画模式**
   2~3 人同时画同一个词（各自画布），猜者从中选一幅来猜；猜中时对应画手加分。相当于"画手内卷"。
   *实现要点*：`room.strokes` 变成 `Map<playerId, strokes>`，广播时带上 `ownerId`。

8. **观众竞猜**
   已经猜中 / 在等待的人，可以对"本轮能否被猜中"下注（赢 +20 / 输 -10），把冷场时间变成参与时间。

9. **跨局成就系统**
   连续 3 局首猜、累计画满 50 个词、单局零重复、单局画作满分……存 `localStorage`，结算页弹成就。
   这一条对"多轮游戏中保持耐玩度"特别有效。

### P2 · 新模式，工作量大

10. **词语接龙模式**：本次画的答案必须包含上一轮答案的最后一个字。
11. **emoji 猜词模式**：不用画，用 3 个 emoji 做提示，比谁先猜中（手机端体验极好）。
12. **双人共画**：两人共用一块画布画同一个词，但各自只能画自己那一半。
13. **AI 裁判**：接一个大模型判断"画得像不像"，给相似度分（需要外部 API，会破坏零依赖）。
14. **回放与画廊**：`room.strokes` 已经完整存了每回合笔画，局末可生成一张长图或 GIF 回放，方便分享。

### P3 · 细节打磨

15. **音效分层**：不同难度用不同提示音，倒计时最后 5 秒加心跳声。
16. **反作弊**：同一 IP 多开限制；答案目前只存在服务端（已验证猜者拿不到明文），保持即可。
17. **投票细化**：给画作投票加维度（像不像 / 好笑 / 用心），或者让画手也能投"最佳猜手"。

---

## 六、文件清单与测试

### 新增 / 修改

| 文件 | 说明 |
|---|---|
| `words.js` | **新增** 词库（578 词 / 20 分类），按分类→难度分组 |
| `picker.js` | **新增** 选词引擎 v2（三层防重复 + 难度配比 + 分类去重 + 自定义词合并） |
| `server.js` | 改用上述模块；主题解析（随机/每日）、自定义词清洗、限制玩法强制、画作投票、连击、追赶、开天眼、结算称号与统计 |
| `public/index.html` | 大厅：主题选择 / 限制开关 / 自定义词导入三块；工具栏「开天眼」；表情条移出工具栏；结算页统计区；回合末星级投票区 |
| `public/app.js` | 主题 chips（含快捷主题互斥）、限制开关、自定义词导入、星级投票状态机、连击角标、词池进度、称号渲染 |
| `public/style.css` | chips / 快捷主题 / 星级 / 投票区 / 结算统计样式；`.bottom` 布局 |
| `test/wordpool.js` | **新增** 选词重复率压测（老库/新库/v2 三方案对比） |
| `test/client.js` | **新增** 可复用的 WebSocket 测试客户端 |
| `test/v2.js` | **新增** 玩法集成测试（主题、连击、开天眼、称号、零重复） |
| `test/features.js` | **新增** 新玩法特性集成测试（随机/每日主题、自定义词、限制玩法、投票） |
| `test/dom.js` | **新增** 前端 DOM 冒烟测试（jsdom 真实执行 app.js） |
| `test/tunnel-check.js` | **新增** 公网隧道自检（HTTP + WebSocket + 完整回合） |
| `test/launcher.js` | **新增** 桌面启动器自检（菜单 / 前置检查 / 分支兜底） |
| `test/latency.js` | **新增** 延迟测量（本机回环 vs 公网隧道，裸往返 + 笔画端到端） |
| `test/tunnel-probe.js` | **新增** 走 TLS 的 WebSocket 探针（隧道测量用） |
| `test/cdp.js` | **新增** 极简 Chrome DevTools Protocol 驱动（零依赖，用 Node 自带 WebSocket） |
| `test/responsive.js` | **新增** 多设备真实渲染验证（9 视口 + 真触摸绘制 + 15 张截图） |
| `public/compat.js` | **新增** 浏览器兼容层 + 设备识别（ES5 写成，老内核可跑） |
| `test/run-all.js` | **新增** 一键跑全部测试 |
| `test-all.bat` | **新增** 双击即可跑测试 |
| `start-public.bat` | **新增** 双击即开公网隧道（校园网联机的正解） |
| `start-public.js` | **新增** 公网联机启动器（自动抓地址 + 剪贴板 + 掉线重连） |
| `start-frp.bat` + `frp/` | **新增** 国内中转联机（frp 0.64.0）：客户端/服务端二进制、双端配置、部署指南、启动器 |
| `launch.js` | **新增** 启动选择器：桌面双击后选「公网 / 局域网」 |
| `public-url.txt` | **新增** 当前公网地址落盘，忘了随时翻 |
| `tools/cloudflared.exe` | **新增** Cloudflare 隧道客户端（v2026.9.1，走 gh-proxy 镜像下载） |
| `package.json` | **新增** 只含 scripts，无依赖，不影响零依赖特性 |
| `桌面\文件\你画我猜.bat` | **改写** 原来直接走局域网（校园网里等于点不动），现在改成调 `launch.js` 出选择菜单；原文件备份为 `你画我猜.bat.bak` |

### 怎么跑测试

双击 `C:\Users\Administrator\DrawGuess\test-all.bat`，或者：

```bash
cd C:\Users\Administrator\DrawGuess
npm test          # 一键跑全部（会自动拉起测试端口，跑完自动关闭）
```

最近一次全量结果：

```
================ 汇总 ================
  ✅  词库 & 选词引擎压测          12 项
  ✅  桌面启动器自检               10 项
  ✅  前端 DOM 冒烟                97 项
  ✅  端到端回归                   46 项
  ✅  v2 玩法集成                  22 项
  ✅  新玩法特性集成               40 项
  ✅  多设备真实渲染               97 项   ← headless Chrome + CDP
=====================================
             合计 324 项断言全部通过
```

其中**端到端回归 46 项覆盖原有全部行为**（换词额度、自定义出题、接近提示、画手禁言、断线处理等），除「换词默认次数从 2 改成 3」这一项按新设定更新外，其余行为零回归。

> 静态资源（`index.html` / `style.css` / `app.js`）是**每次请求现读磁盘**且带 `Cache-Control: no-cache`，
> 所以改前端**不用重启服务**，刷新页面即可生效；只有改 `server.js` / `picker.js` 才需要重启。

### 可调参数（都在 `server.js` 顶部常量区）

| 常量 | 默认值 | 含义 |
|---|---|---|
| `COMBO_BONUS` / `COMBO_MAX` | 12 / 5 | 连击每层加分 / 层数上限 |
| `CATCHUP_MAX` / `CATCHUP_GAP` | 0.40 / 600 | 追赶加成上限 / 吃满加成所需分差 |
| `REVEAL_COST` | 15 | 开天眼每次扣分 |
| `VOTE_SCORE_PER_STAR` | 8 | 画作每星折算分 |
| `CUSTOM_WORD_MAX` | 200 | 单房自定义词上限 |
| `REROLL_DEFAULT` / `REROLL_OPTIONS` | 3 / [2,3,5] | 换词默认次数 / 房主可选档位 |
| `RECENT_MAX`（`picker.js`） | 80 | 跨房间软冷却窗口 |
| `TURN_END_DELAY` / `VOTE_WINDOW_MS` | 6500 / 3900 | 回合结算停留 / 其中投票窗时长 |

想调手感直接改数字即可，改完跑 `npm test` 确认没踩到边界。

---

## 七、联机排障：局域网连不上怎么办

### 症状
另一台设备打开 `http://172.16.26.194:3000` 报 **ERR_CONNECTION_REFUSED**，但服务器窗口显示服务正常。

### 实测诊断结果

| 检查项 | 结果 | 结论 |
|---|---|---|
| 端口监听 | `0.0.0.0:3000 LISTENING`（PID 19660） | ✅ 监听在所有网卡，不是绑定问题 |
| 防火墙 · 域/专用/公用 | **三个配置文件全部"关闭"** | ❌ 不是防火墙 |
| 防火墙入站规则 | `DrawGuess 3000`、`DrawGuess3000` 两条 TCP 3000 允许规则 | ✅ 规则早就有了 |
| 本机访问 LAN 地址 | `msedge.exe` 成功建立 ESTABLISHED 连接 | ✅ 地址本身可用 |
| 网卡与子网 | `172.16.26.194/255.255.248.0` → 网段 `172.16.24.0/21`，网关 `172.16.28.1` | ⚠️ **典型校园网/企业网段** |

**根因：校园网（或公司网）在接入交换机上开了「客户端隔离 / 端口隔离 / AP 隔离」**，同网段设备之间互相看不见。这是网络侧策略，本机怎么设置都改不了——所以关防火墙、加放行规则都不会有任何效果。

### 解决方案

#### 方案 A：公网隧道（推荐，最彻底）
用 Cloudflare 免费隧道把本机 3000 端口暴露到公网，**任何网络环境的人都能加入**（校园网、4G/5G、异地都行）。

**日常就用桌面那个入口**：`桌面\文件\你画我猜.bat`（已改写）。双击后出菜单：

```
==========================================================
   你画我猜 · 选择联机方式
==========================================================

     [1]  公网联机   ← 推荐，校园网 / 异地都能连
     [2]  局域网联机   仅同一局域网内可用

   10 秒内没选择，自动走公网联机。

  请输入 1 或 2 后回车：
```

直接回车或乱按都会兜底走公网；也可以直接传参跳过菜单：

```bash
node launch.js --public   # 直接公网
node launch.js --lan      # 直接局域网
```

也可以单独跑 `start-public.bat`（逻辑在 `start-public.js`），它会自动完成：

1. **探测 3000 端口**——有服务就直接复用，没有就自动拉起 `server.js` 并等它就绪；
2. **建立隧道**，从 cloudflared 输出里**自动抓取** trycloudflare 地址；
3. **大字打印地址**，同时**复制到剪贴板**并写入 `public-url.txt`（忘了地址随时翻这个文件）；
4. **掉线自动重连**（最多 5 次），关窗口时一起退出。

```
================================================================
   把这个地址发给朋友，他们用浏览器打开就能玩：

        https://great-mounting-daughter-partners.trycloudflare.com

   （已复制到剪贴板，也存了一份到 public-url.txt）
================================================================
```

> 💡 更省事的做法：让朋友打开隧道地址后，**登录页右下角会直接显示当前可分享的地址**（页面读的是 `location.host`），房主照抄即可，不用去翻控制台。

隧道可用性已实测（`test/tunnel-check.js`）：

```
✓ HTTP 页面能通过隧道打开          -> HTTP 200 · 8945 字节
✓ 页面没有被插入拦截页              （Cloudflare 无 click to continue 拦截页）
✓ WebSocket 握手穿过隧道成功        -> 房间号 4958
✓ 隧道下服务端下发词库信息          -> 578 词
✓ 第二个客户端能加入同一房间
✓ 隧道下能正常指派画手并下发候选词   -> 手机 / 兵马俑 / 射箭
✓ 笔画能实时同步过隧道（画手→猜者）  -> 收到 op=begin color=#ef4444
✓ 猜词判定过隧道正常                -> +196 分
✓ 回合结算过隧道正常                -> 答案「手机」
```

**注意事项**
- 免费隧道**每次重启 URL 都会变**，而且没有可用性保证（Cloudflare 官方声明）。开黑前现开现发即可。
- **隧道掉线重连后地址也会变**。启动器会自动抓新地址、重写 `public-url.txt` 并复制到剪贴板，
  但**之前发出去的旧链接会失效**（实测旧地址会返回 HTTP 530）。
  所以别收藏旧链接——每次开局让朋友现点登录页上那个地址最稳。
- 隧道期间游戏对公网可见。房间号只有 4 位，陌生人理论上能猜到房间——**别在隧道开着的时候聊私密内容**。想更安全可以用 Cloudflare 具名隧道 + Access 策略（需要账号）。
- 关掉隧道窗口 = 立即断线，别忘了。

#### 方案 B：手机热点（不用装任何东西）
把两台设备都连到同一个**手机热点**。热点是 NAT 后的私有网段，没有客户端隔离，局域网地址立刻可用。
缺点：热点通常只有 2.4G/5G 内网，且设备数量有限。

#### 方案 C：换网络
如果学校有允许设备互访的 SSID（有些学校的"宿舍网"不隔离，只有教学区隔离），换过去即可。

#### 方案 D：正规做法
如果有 Cloudflare 账号，可以用具名隧道绑定固定域名，URL 不变、稳定性更好：

```bash
tools\cloudflared.exe tunnel login
tools\cloudflared.exe tunnel create drawguess
tools\cloudflared.exe tunnel route dns drawguess drawguess.你的域名.com
tools\cloudflared.exe tunnel run --url http://localhost:3000 drawguess
```

### 顺手补的两件事

1. **服务端启动横幅**现在会提示这个坑：局域网连不上时该去双击 `start-public.bat`。
2. **新增 `test/tunnel-check.js`**：一条命令验证任意隧道地址能不能承载联机游戏（HTTP + WebSocket 握手 + 笔画同步 + 猜词判定 + 回合结算）。

```bash
node test/tunnel-check.js https://xxxx.trycloudflare.com
```



## 八、电脑端 / 手机端全面适配

### 8.1 设备识别：`data-layout` 驱动，不用媒体查询

新增 **`public/compat.js`**，在 `<head>` 里**同步执行**（先于首次绘制，不会闪一下桌面版），把识别结果写到 `<html data-layout="mobile|desktop">`，**页面样式全部由这个属性驱动**。

为什么不用媒体查询？因为要同时支持「自动识别」和「用户手动切换」。媒体查询没法被手动覆盖，而 `data-layout` 可以——**JS 与 CSS 用同一个判断口径，不会出现两套逻辑打架**。

`auto` 判定规则：

| 条件 | 结果 | 覆盖场景 |
|---|---|---|
| 触摸设备 **且** 短边 ≤ 500px | 手机版 | 手机竖屏 + **横屏**（横屏宽度会超过断点，只看宽度会误判） |
| 窗口宽度 ≤ 880px | 手机版 | 窄窗口、平板竖屏（iPad mini 768） |
| 其余 | 电脑版 | 平板横屏（1024×768）、笔记本、外接显示器 |

`compat.js` 同时输出 `data-device`（touch/mouse）、`data-engine`（blink/webkit/gecko/edge/opera/trident）、`data-flexgap`，方便日后定位兼容问题。

### 8.2 兼容层做了什么

`compat.js` **刻意写成 ES5**（不用箭头函数 / `const` / 模板字符串 / `?.` / `??`）——它要在老浏览器上跑，自己不能先用新语法。补齐的能力：

| 补齐项 | 谁会缺 |
|---|---|
| `ResizeObserver`（用 resize 事件兜底） | iOS 13 以下、老 Android WebView |
| `Element.remove` / `matches` / `Array.from` / `Array.includes` / `String.padStart` / `Object.assign` | 老内核 |
| `requestAnimationFrame` | 很老的内核 |
| **flex `gap` 探测**（不支持时给 `<html>` 加 `.no-flex-gap`，用外边距兜底关键几行） | **iOS 14.1 以下、Android WebView 84 以下** |
| `--vh` / `--app-h` 视口高度变量 | 所有移动端（地址栏伸缩、软键盘） |

客户端代码里唯一一处 `?.`（`bs[n]?.click()`）也换成了显式判空——**现在 `app.js` / `compat.js` 里没有任何 ES2020+ 语法**。

### 8.3 输入层：pointer / touch / mouse 三合一

原来只用 Pointer Events。**iOS 13 以下和老 Android WebView 根本没有 `PointerEvent`**，那些设备上画布是画不了的。

现在统一成一层：

- 有 `PointerEvent` → 走 pointer 事件；
- 没有 → 退回 `touchstart/move/end` + `mousedown/move/up`；
- `{ passive: false }` 做能力探测（老浏览器会把选项对象当成 `useCapture` 布尔值，直接写会导致监听器挂错阶段）；
- **`activeId` 只认第一根手指** —— 手掌边缘碰到屏幕不会打断笔画（手掌误触在手机上非常常见）；
- 只有「是画手 + 回合进行中 + 画板未锁」时才 `preventDefault`，其余情况不拦，页面还能正常滚。

### 8.4 手机端布局

| 改动 | 为什么 |
|---|---|
| `.stage` 在上、`.side` 在下（纵向堆叠） | 手机宽度放不下 274px 侧栏 + 画布 |
| 底部面板**可折叠**，画手回合自动折叠、猜词者回合自动展开 | 画手要整块画布，猜词者要打字框——按角色自动切 |
| 工具栏 + 表情合并成**一行横向滚动** | 原来换行占两三行，垂直空间不够 |
| 触摸目标放大：工具栏 44×40、粗细 42×40、表情 40×40、颜色点 30 | 原来 30px 左右，手指点不准 |
| **输入框字号强制 ≥16px** | 否则 iOS Safari 聚焦时会把页面放大，布局直接崩 |
| `env(safe-area-inset-*)` | 刘海屏 / 底部横条遮挡 |
| 覆盖层改为可滚动 + 顶部对齐 | 选词弹窗在矮屏上会把按钮顶出屏幕 |
| 大厅「开始游戏」**吸底** | 大厅很长，手机上要滚很久才能开局（实测 390×844 下按钮在 y=1206） |
| 画布 `devicePixelRatio` **压到 ≤2**，且总像素 ≤420 万 | 不少安卓机报 dpr 3~4，按原值分配会得到上千万像素，rAF 重绘卡成幻灯片 |

### 8.5 移动端其他加固

- `overscroll-behavior: none` —— 防止画图时**下拉刷新**把整局刷掉；
- `touch-action: manipulation` —— 干掉双击缩放和 300ms 点击延迟；
- `touch-action: none` 保留在画布上 —— 画图时页面绝不滚动；
- **音频解锁**：iOS 要求 `AudioContext` 由用户手势创建/恢复，否则全程静音。现在首次触摸/点击时解锁一次；
- `-webkit-backdrop-filter` / `-webkit-overflow-scrolling` / `-webkit-text-size-adjust` 等前缀；
- `format-detection: telephone=no` —— 否则 iOS 会把 4 位房间号识别成电话号码变成链接；
- 手机版**不自动聚焦昵称输入框**（否则一进页面就弹键盘）；
- 软键盘弹出时靠 `visualViewport` 更新 `--vh`，并把聊天输入框滚进可视区。

### 8.6 验证方式：真浏览器 + 真触摸

**不是靠肉眼看代码，是拿本机 Chrome 真的渲染出来测的。** 新增 `test/cdp.js`（极简 CDP 驱动，用 Node 22 自带的 `WebSocket`/`fetch`，**不需要 playwright/puppeteer，也不用下载浏览器**）+ `test/responsive.js`。

覆盖 **9 种视口**，每种都检查设备识别、横向溢出、卡片越界、`--vh`、分享地址、切换按钮：

```
desktop-1440 / laptop-1280 / tablet-1024 / ipad-768 / iphone14-390
iphoneSE-375 / android-360 / narrow-320 / landscape-844x390
```

游戏页检查（390×844 手机视口实测）：

```
✓ 画布在面板上方（纵向堆叠）   -> stage.top=0 side.top=569
✓ 画布尺寸正常                -> 374×383 css / 748x766 位图
✓ devicePixelRatio 已压到 ≤2   -> dpr=2.00
✓ 输入框字号 ≥16px            -> 16px
✓ 工具栏/粗细/表情按钮 ≥40px   -> 40px
✓ 颜色圆点 ≥28px              -> 30px
✓ 无横向溢出                  -> scrollWidth 390 vs 390
✓ 画布 touch-action:none
```

桌面 1440×900 实测：

```
✓ 侧栏与舞台左右并排   -> side.left=0 stage.left=274
✓ 画布尺寸正常         -> 1142×687
✓ 折叠条隐藏（手机版专属）
✓ 视口变窄后自动切成手机版（无需刷新）-> stage.top=0 side.top=569
```

**真触摸绘制**（用 `Input.dispatchTouchEvent` 派发真实触摸序列，走浏览器输入管线，不是直接调 JS）：

```
✓ 触摸事件真的画出了笔画并同步到对手
   -> 对手收到 draw: begin,pts,pts,pts,pts,pts,pts,end
✓ 触摸笔画坐标在 0~1 归一化范围内   -> x=0.200 y=0.500
```

截图产物在 `screenshots/responsive/`（15 张），包含 9 种登录页视口、手机大厅、手机游戏页、手机绘制中、桌面游戏页、以及「手机上强制切电脑版」的对照图。

### 8.7 老内核降级：真的删掉 API 来验证

「支持老浏览器」不能靠嘴上说。CDP 提供了 `Page.addScriptToEvaluateOnNewDocument`，可以在**页面脚本执行前**把现代 API 删掉，模拟出 iOS 12 / 老 Android WebView 的环境：

```js
Object.defineProperty(window, 'PointerEvent',     { value: undefined });
Object.defineProperty(window, 'ResizeObserver',   { value: undefined });
Object.defineProperty(window, 'visualViewport',   { value: undefined });
Object.defineProperty(navigator, 'clipboard',     { value: undefined });
```

在这个**残缺环境**里跑完整流程，实测结果：

```
✓ 降级环境已注入           -> PointerEvent=false visualViewport=false clipboard=false
✓ ResizeObserver 被 compat 兜底实现接管   -> roIsPolyfill=true
✓ 老内核下 compat.js 仍能识别设备并定布局  -> data-layout=mobile
✓ 老内核下 --vh 回退到 innerHeight 仍算得出来 -> --vh=8.44px
✓ 老内核下登录页无横向溢出
✓ 无 clipboard API 时复制退回 execCommand  -> 按钮文案「已复制 ✓」
✓ 老内核下能创建房间 / 进游戏页
✓ 无 PointerEvent 下触摸画图仍然可用
   -> 对手收到 draw: begin,pts,pts,pts,pts,end     ← 走的是 touch 回退分支
✓ 老内核下拉伸窗口能重排成桌面版（ResizeObserver 兜底生效）
   -> sideBySide=true 画布宽 1142
```

**这一组是最有说服力的证据**：`PointerEvent` 被删掉后画图仍然工作，说明 touch + mouse 回退分支是真跑通的，不是摆设。

### 8.8 适配程度总表（截至当前）

**✅ 已实测通过**

| 维度 | 覆盖 | 证据 |
|---|---|---|
| 视口 | 1440 / 1280 / 1024 / 768 / 390 / 375 / 360 / 320 / 横屏 844×390，共 **9 种** | `test/responsive.js` 真实渲染断言 |
| 布局自动切换 | 触摸+短边≤500 或 宽≤880 → 手机版；其余电脑版 | 9 种视口逐一断言 `data-layout` |
| 手动切换 | 手机↔电脑，记忆到 `localStorage`，可恢复自动 | 实测切过去再恢复 |
| 实时重排 | 视口变化/旋转，**无需刷新** | 桌面→手机 一次切换后重新量算 |
| 触摸绘制 | 真 `Input.dispatchTouchEvent` 序列 → 对手收到 `begin,pts,…,end` | 真触摸端到端 |
| 触摸目标 | 工具栏/粗细/表情 ≥40px、颜色点 30px、小按钮 ≥34px | 逐元素量取 |
| iOS 聚焦缩放 | 输入框字号强制 ≥16px | 实测 16px |
| 性能 | 画布 dpr 压到 ≤2 且总像素 ≤420 万 | 实测 dpr=2.00 |
| 无横向溢出 | 9 种视口 + 游戏页 + 大厅 | `scrollWidth` vs `innerWidth` |
| **老内核降级** | 删掉 `PointerEvent`/`ResizeObserver`/`visualViewport`/`clipboard` 后**全流程仍可用** | 触摸画图、复制、重排逐项验证 |
| flex gap 兜底 | 不支持时自动加 `.no-flex-gap` 用外边距顶上 | 能力探测已执行 |
| 安全区 | 刘海/底部横条 `env(safe-area-inset-*)` | 已接入各关键容器 |
| 下拉刷新 | `overscroll-behavior: none` 防止刷掉整局 | 已生效 |
| 音频 | iOS 手势解锁 `AudioContext` | 首次触摸/点击解锁 |
| 隧道可靠性 | 30 秒心跳 + 断线重建 + `tunnel.log` | 启动即报「连通性正常 ✓」 |

**⚠️ 未在此环境实测，但已针对性处理**

| 环境 | 已做的处理 | 无法验证的原因 |
|---|---|---|
| iOS Safari | `-webkit-` 前缀、`visualViewport`、音频手势解锁、16px 防缩放、`format-detection:telephone=no`（否则 4 位房间号被识别成电话号） | 本机没有 Safari |
| 微信内置浏览器 | 不依赖 `navigator.clipboard`、不依赖新 API、UA 已识别（`data-wechat`） | 本机没有微信 |
| Firefox / Gecko | 无 `-webkit-` 依赖、标准 API 优先 | 本机没装 |
| 老 Android WebView | 已用「删 API」方式模拟验证 | 无法覆盖每个厂商定制版 |

**❌ 明确不支持的**

- IE 11 及更早：缺 `Promise` / `WebSocket`，补不动也不值得补。
- 关闭 JavaScript：这个游戏靠 WebSocket 通信，没有 JS 跑不起来（布局也依赖 `compat.js` 设置 `data-layout`）。

**结论一句话**：**Blink 内核（Chrome / Edge / 绝大多数安卓浏览器，含 Via、UC、QQ、夸克这类用系统 WebView 的）+ 模拟的老内核环境，已经实测全绿；iOS Safari 和微信是按已知坑逐条处理过、但没法在这里实测——上线前务必拿真机用微信走一局。**

### 8.9 老实说清楚边界

- **能验证的**：Blink 内核（Chrome / Edge / 绝大多数安卓浏览器，含 Via / UC / QQ / 夸克这类用系统 WebView 的）+ **模拟的老内核环境**（无 PointerEvent / ResizeObserver / visualViewport / clipboard）—— 全部实测通过。
- **没法在这里验证的**：iOS Safari、微信内置浏览器、Firefox。本机没有这些环境，**我不会假装测过**。
  针对它们的已知坑都做了处理：`-webkit-` 前缀、`visualAccess`→`visualViewport`、音频手势解锁、flex gap 兜底、PointerEvent 回退、输入框 16px 防缩放、`format-detection`。
- **建议**：上线前拿一台安卓机用**微信**打开走一局——微信 WebView 是最容易出问题的环境。

### 8.10 隧道可靠性：心跳看门狗

之前踩过一次：**cloudflared 整个进程挂掉**，启动器的 `exit` 事件没兜住（进程被杀时不一定触发），结果公网地址直接 404 了。

现在 `start-public.js` 加了**主动心跳**：每 30 秒真的 HTTP 探一次隧道地址，连续 2 次失败就杀掉重建；连续 3 次健康后把重试计数清零（避免长期运行被累计上限卡死）。另外：

- 地址发布后先立刻探一次，确认隧道真的通了才提示「连通性正常 ✓」；
- 全过程写 `tunnel.log`（启动 / 换地址 / 断开 / 重建 / 心跳失败），出问题能回溯。


---

---

## 九、实时绘制延迟：实测分析与优化

### 9.1 先量出来，别猜

新增 `test/latency.js`（协议里加了 `ping/pong`，纯回环，专门量往返），分别在**本机回环**和**公网隧道**上测：

| 链路 | 裸往返 p50 | p90 | max |
|---|---|---|---|
| 本机回环 | **0.37 ms** | 0.54 ms | 0.63 ms |
| 公网隧道（QUIC） | **354.6 ms** | 355.7 ms | 359.3 ms |
| 公网隧道笔画端到端 | **373.4 ms** | 377.8 ms | 592 ms |

> ⚠️ 第一版脚本把本机回环量成了 30ms——因为 `wait()` 是 20ms 轮询，**测量精度被自己的轮询拖累了**。
> 改成事件驱动（收到消息立刻 resolve）后回环降到 0.37ms。**测量工具本身有偏差，差点把结论带歪。**

### 9.2 355ms 花在哪

**结论：几乎全在隧道路径上，客户端只占约 19ms。**

```
笔画端到端 373ms  =  网络往返 355ms  +  客户端发送缓冲 ~18ms
```

- **网络 355ms（占 95%）**：cloudflared 免费快速隧道由 Cloudflare 调度接入点，实测注册到 `location=lax11`（**洛杉矶**）。每帧数据要绕 中国 → 美西 → Cloudflare 边缘 → 美西 → 中国。跨太平洋一来回 350ms 完全对得上。
- **客户端缓冲 ~18ms**：落笔后原本固定等 45ms 才发第一批点，这是纯代码造成的延迟。
- **抖动（比平均值更伤体验）**：隧道刚建立时实测 p90 575ms、max **1990ms**——**包是一阵一阵到的**。即使平均 355ms，观感也是"一顿一顿"，比稳定 400ms 难受得多。

### 9.3 已实施的优化

| # | 优化 | 原理 | 效果 |
|---|---|---|---|
| 1 | **接收端渐进显形（核心）** | 收到整批点不立刻全画出来，让 `revealed` 按指数逼近（每帧追 22% 剩余量）逐点显形 | 抖动被抹平成匀速笔触；局域网下几乎无感 |
| 2 | **落笔头 140ms 用 16ms 间隔** | 原本固定 45ms 才发第一批点 | 感知的"按下去有没有反应"少约 29ms |
| 3 | **坐标保留 4 位小数** | 1000px 画布上精度 0.1px，肉眼无差别；JSON 体积小约 1/3 | 每帧少传字节，长笔画不易触到 MTU |
| 4 | **界面延迟指示器** | 每 4 秒 `ping` 一次，滑动平均后显示，绿/黄/红三档 | 用户能立刻判断"卡是网络的锅还是游戏的锅" |

**防累积兜底**：渐进显形落后超过 260 个点就**直接追平**，避免延迟越积越大（`SMOOTH_LAG_MAX`）。本地笔迹带 `local` 标记**不参与平滑**——自己画的必须零延迟回显。

### 9.4 试过但不可行的方案（附实测）

| 方案 | 结果 | 原因 |
|---|---|---|
| `cloudflared --region ap`（换亚太节点） | ❌ **直接失败** | 校园网 DNS 解析不了 `_ap-v2-origintunneld._tcp.argotunnel.com` 的 SRV 记录（全球区域却可以）。日志原文：`lookup argotunnel.com: no such host` |
| `--protocol http2` 替代 QUIC | ⚠️ **中位数略好、抖动翻倍** | p50 336ms（-19ms），但 p90 从 356ms 恶化到 **763ms**、max 1067ms。TCP 队头阻塞在大延迟路径上更致命。**保持默认 QUIC** |

### 9.5 又验证了一遍：换 Cloudflare 节点救不了

在换方案之前，先确认「能不能就近连上 Cloudflare」。实测各 anycast 段的 TCP 握手耗时：

| 目标 | TCP 握手 RTT |
|---|---|
| **114DNS（国内基线）** | **8 ms** |
| Cloudflare `162.159.192.1`（实测最优） | 142 ms |
| Cloudflare `198.41.192.1` | 144 ms |
| Cloudflare `104.16.0.1` | 149 ms |
| Cloudflare `104.18.0.1` | 150 ms |
| 隧道实际使用的 `104.16.230.132` | 154 ms |
| Cloudflare `172.65.0.1` / `188.114.96.1` / `104.26.0.1` | 187 / 201 / 193 ms |

**结论（关键）**：
- 校园网本身**非常快**——国内 RTT 只有 **8ms**；
- 但到 Cloudflare **最近的边缘节点也要 142ms**，比国内慢 **18 倍**；
- 因为浏览器和 cloudflared 在**同一台机器**上，路径是「本机 → 边缘 → 本机」，
  所以 **355ms ≈ 2 × 143ms + 开销**，完全对得上；
- **Cloudflare 在这台机器上的物理下限就是约 286ms**——把所有节点试遍，
  最好的也只能省 12ms。**换节点这条路彻底堵死。**

### 9.6 真正的解法：国内中转（已做成开箱即用）

既然国内 RTT 只有 8ms，把中转服务器放到国内，延迟能降一个数量级。

**已经全部准备好，只差一台服务器：**

| 文件 | 用途 |
|---|---|
| `frp/frpc.exe` | Windows 客户端（本机跑） |
| `frp/frpc.toml` | 客户端配置 —— **只需改 `serverAddr` 一行**（token 已配好） |
| `frp/frps-linux-amd64` | Linux 服务端二进制（传到 VPS） |
| `frp/frps.toml` | 服务端配置（含随机 token、`allowPorts` 只开 3000） |
| `frp/README.md` | **完整部署指南**（含 systemd 常驻配置、常见问题） |
| `start-frp.bat` | 双击即用，自动拉起服务 + 连中转 + 打印并复制分享地址 |

预期效果：

| 链路 | RTT |
|---|---|
| Cloudflare 隧道（现在） | 355 ms |
| **国内 frp 中转（预期）** | **10~40 ms** |

**验证方式**：进游戏后看顶栏右上角的延迟指示器——**现在应该是红的，换 frp 后应该变绿**。

**为什么 P2P 直连不行**：校园网在接入交换机上开了客户端隔离（见第七章），
设备之间互相看不见。但 frp 是**游戏机主动连出去**，正好绕开这个限制。

**成本**：阿里云/腾讯云学生机约 10 元/月；轻量服务器新用户促销约 50~90 元/年。不需要域名、不需要备案。

**P1 · 继续压榨感知延迟**

- **外推预测**：接收端根据最后几个点的方向/速度预测下一个点先画出去，真实数据到了再修正。能把**感知**延迟再降 100~150ms，但快速转向时会出现"甩尾"错觉，需要配合平滑回拉。
- **自适应发送频率**：按测得的 RTT 动态调发送间隔——RTT 高时少发点、让接收端多插值，减少拥塞与抖动。
- **笔画压缩编码**：JSON 数字数组改成 delta + base36 定点编码，单点从约 12 字节压到约 4 字节。当前 355ms 路径下带宽不是瓶颈，**换成国内中转追求极限时再上**。

**P2 · 协议层**

- **WebRTC DataChannel（`maxRetransmits: 0`，unreliable/unordered）**：彻底消除队头阻塞，是实时绘制的正解。但校园网 P2P 被隔离，必须配 TURN 中继——而 TURN 同样得放国内才有意义。**本质上和 P0 是同一件事，复杂度高得多**，建议先做 P0。

### 9.7 结论

1. **公网延迟 355ms 的根因是隧道路径绕了美西，不是游戏代码。** 代码侧只贡献约 18ms。
2. **换 Cloudflare 节点救不了**：国内 RTT 8ms，但到 Cloudflare 最近的边缘节点要 142ms（慢 18 倍），
   而隧道要往返两次边缘 → 物理下限约 286ms。把所有 anycast 段试遍，最好也只能省 12ms。
3. **已做的优化不降低 RTT，但显著改善观感**——抖动被抹平，笔画从"一顿一顿"变成匀速笔触。
4. **想真正把延迟打下来，唯一有效的办法是把中转服务器放到国内**（预期 10~40ms，比现在快 9~35 倍）。
   相关文件、配置和部署指南都已经准备好了（见 9.6）。
5. 界面上的延迟指示器让你随时能验证以上结论：**局域网绿、公网隧道红，一眼可辨。**
